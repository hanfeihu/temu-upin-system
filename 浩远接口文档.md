# HY Express 接口文档整理

来源：`https://order.hy-express.com/usercenter/manager/api_document.aspx`
抓取时间：2026-04-13
统一请求地址：`http://order.hy-express.com/webservice/PublicService.asmx/ServiceInterfaceUTF8`
统一请求方式：`POST`
统一 Content-Type：`application/x-www-form-urlencoded`

## 通用请求参数

所有接口统一提交以下表单字段：

- `appToken`：API账号
- `appKey`：API密码
- `serviceMethod`：接口方法名
- `paramsJson`：JSON 字符串格式的业务参数

---

## 1. 创建订单 `createorder`

### 必填顶层字段
- `reference_no`：客户参考号
- `shipping_method`：运输方式代码
- `shipper`：发件人信息
- `consignee`：收件人信息
- `invoice`：申报信息数组

### 常用字段
- `shipping_method_no`：服务商单号
- `order_weight`：订单重量 KG，默认 `0.2`
- `order_pieces`：外包装件数，默认 `1`
- `cargotype`：货物类型，`W`包裹 / `D`文件 / `B`袋子
- `order_status`：`P`已预报（默认）/ `D`草稿
- `mail_cargo_type`：申报种类，`1`礼品 / `2`商品货样 / `3`文件 / `4`其他（默认 `4`）
- `buyer_id`：买家ID
- `order_info`：订单备注
- `platform_id`：平台ID或平台CODE
- `custom_hawbcode`：自定义单号
- `delivery_label_url`：尾程派送标签URL
- `production_sales_company`：生产销售企业单位
- `production_sales_companycode`：生产销售企业代码
- `cargovolume`：材积信息数组
- `extra_service`：额外服务数组

### shipper 发件人字段
- `shipper_name` 必填
- `shipper_company`
- `shipper_countrycode` 必填，二字代码
- `shipper_province`
- `shipper_city`
- `shipper_district`
- `shipper_street` 必填
- `shipper_postcode`
- `shipper_areacode`
- `shipper_telephone` / `shipper_mobile` 至少传一项
- `shipper_email`
- `shipper_fax`

### consignee 收件人字段
- `consignee_name` 必填
- `consignee_company`
- `consignee_countrycode` 必填，二字代码
- `consignee_province` 条件必填
- `consignee_city` 条件必填
- `consignee_district` 条件必填
- `consignee_street` 必填
- `consignee_postcode` 条件必填
- `consignee_doorplate`
- `consignee_areacode`
- `consignee_telephone` / `consignee_mobile` 至少传一项
- `consignee_email`
- `consignee_fax`
- `consignee_certificatetype`：`ID`身份证 / `PP`护照
- `consignee_certificatecode`
- `consignee_credentials_period`
- `consignee_tariff` 条件字段
- `consignee_eori_number` 条件字段

### invoice 申报字段
- `sku`
- `invoice_enname` 必填
- `invoice_cnname` 条件字段
- `invoice_quantity` 必填
- `unit_code`：默认 `PCE`，可如 `MTR` / `PCE` / `SET`
- `invoice_unitcharge` 必填
- `net_weight`
- `hs_code`
- `register_code`
- `invoice_note`
- `invoice_url`
- `invoice_info`
- `invoice_material`
- `invoice_spec`
- `invoice_use`
- `invoice_brand`
- `posttax_num`

### 成功返回重点
- `success`：`1`成功，`2`重复订单，`0`失败
- `data.order_id`
- `data.refrence_no`
- `data.shipping_method_no`
- `data.channel_hawbcode`

---

## 2. 提交预报 `submitforecast`

用于把草稿订单提交预报。

### paramsJson
- `reference_no` 必填
- `order_weight` 可选

### 返回
- `success`
- `data.order_id`
- `data.refrence_no`
- `data.shipping_method_no`
- `data.channel_hawbcode`

---

## 3. 修改订单 `updateorder`

### paramsJson
- `reference_no` 必填
- `order_weight` 必填

### 返回
- `success`
- `cnmessage`
- `enmessage`

---

## 4. 删除订单 `removeorder`

### paramsJson
- `reference_no` 必填

### 返回
- `success`
- `cnmessage`
- `enmessage`

---

## 5. 获取订单标签 `getnewlabel`

### paramsJson
- `configInfo` 必填
  - `lable_file_type`：`1` PNG / `2` PDF
  - `lable_paper_type`：`1` 标签纸 / `2` A4
  - `lable_content_type`：
    - `1` 标签
    - `2` 报关单
    - `3` 配货单
    - `4` 标签+报关单
    - `5` 标签+配货单
    - `6` 标签+报关单+配货单
  - `additional_info`
    - `lable_print_invoiceinfo`：Y/N
    - `lable_print_buyerid`：Y/N
    - `lable_print_datetime`：Y/N
    - `customsdeclaration_print_actualweight`：Y/N
- `listorder` 必填
  - `reference_no` 必填
- `config_code` 可选
  - `1` 标签纸-地址标签
  - `2` 标签纸-地址标签+报关单
  - `3` 标签纸-地址标签+配货单
  - `4` 标签纸-地址标签+报关单+配货单
  - `5` A4纸-地址标签
  - `6` A4纸-地址标签+报关单
  - `7` A4纸-地址标签+配货单
  - `8` A4纸-地址标签+报关单+配货单

### 返回
- `data[].lable_file_type`
- `data[].lable_content_type`
- `data[].lable_file`：标签文件URL

---

## 6. 获取订单跟踪单号 `gettrackingnumber`

### paramsJson
- `reference_no` 必填

### 返回
- `data.refrence_no`
- `data.shipping_method_no`
- `data.channel_hawbcode`

---

## 7. 获取订单跟踪记录 `gettrack`

### paramsJson
- `tracking_number`：服务商单号，必填

### 返回
- `data[].server_hawbcode`
- `data[].destination_country`
- `data[].track_status`
- `data[].track_status_name`
- `data[].signatory_name`
- `data[].details[]`
  - `track_occur_date`
  - `track_location`
  - `track_description`

---

## 8. 获取订单费用（按费用种类汇总）`getbusinessfee`

### paramsJson（三选一）
- `order_id`
- `reference_no`
- `shipping_method_no`

### 返回
- `data[].fee_kind_code`
- `data[].fee_kind_name`
- `data[].currency_code`
- `data[].currency_name`
- `data[].currency_rate`
- `data[].currency_amount`
- `data[].amount`

---

## 9. 获取订单费用明细 `getbusinessfee_detail`

### paramsJson（三选一）
- `order_id`
- `reference_no`
- `shipping_method_no`

### 返回
除汇总费用字段外，还包括：
- `create_date`
- `occur_date`
- `bill_date`
- `note`

---

## 10. 获取订单重量 `getbusinessweight`

### paramsJson（二选一）
- `reference_no`
- `shipping_method_no`

### 返回
- `data.grossweight`
- `data.volumeweight`
- `data.chargeweight`
- `data.volume[]`
  - `box_number`
  - `length`
  - `width`
  - `height`
  - `grossweight`

---

## 11. 费用试算 `feetrail`

### paramsJson
- `country_code` 必填：目的国家二字代码
- `weight` 必填：KG
- `shipping_method`
- `post_code`
- `location`
- `cargo_type`：默认 `W`
- `length`
- `width`
- `height`
- `extra_service`：额外服务代码集合

### 返回
- `data[].ServiceCode`
- `data[].ServiceCnName`
- `data[].ServiceEnName`
- `data[].ChargeWeight`
- `data[].Effectiveness`
- `data[].Formula`
- `data[].FreightFee`
- `data[].FuelFee`
- `data[].RegisteredFee`
- `data[].OtherFee`
- `data[].OtherFeeDetails[]`
- `data[].TotalFee`
- `data[].Remark`
- `data[].Traceability`
- `data[].VolumeCharge`

---

## 12. 获取基础数据

统一入口，按 `serviceMethod` 不同返回不同基础资料：

- `getshippingmethod`：获取运输方式
- `getmailcargotype`：获取申报种类
- `getcountry`：获取国家
- `getcertificatetype`：获取证件类型
- `getdeclareunit`：获取申报单位
- `getextraservice`：获取额外服务
- `getlabelconfig`：获取标签纸张配置

### paramsJson
- 空即可

### 返回结构
- `success`
- `data[]`
  - `code`
  - `cnname`
  - `enname`
  - `note`

---

## 实操要点

1. 统一接口地址固定，核心区别只在 `serviceMethod` 和 `paramsJson`。
2. 所有请求都必须带 `appToken` 和 `appKey`。
3. `paramsJson` 是 JSON 字符串，不是嵌套表单对象。
4. 创建订单时：
   - 如果 `order_status=D`，后续还要调 `submitforecast`。
   - 如果直接正式预报，通常用默认 `P`。
5. 查询类接口经常支持 `reference_no` 或 `shipping_method_no` 二选一/三选一，后面我会优先按你给的参数自动匹配。
6. 文档里部分字段存在拼写 `refrence_no`，这是对方返回字段原样，调用时要按文档兼容处理。

---

## 后续我能直接帮你做的事

你后面只要给我这些信息中的一部分，我就能按文档组装请求：

- `appToken`
- `appKey`
- `serviceMethod` 目标动作（比如创建订单、查单号、查轨迹、试算费用）
- 订单参数（如 `reference_no`、收件人、运输方式、重量、申报信息等）

我会默认按这份文档来生成参数、检查必填、指出缺字段，并在你需要时帮你拼出最终请求体。
