# 测试/部署环境配置（本地开发请优先使用 application-local.yml）
# 使用方式：
#   java -jar ... --spring.profiles.active=dev
server:
  port: 8082

spring:
  datasource:
    # 按需修改为你的数据库
    url: jdbc:postgresql://127.0.0.1:5432/tminos
    username: postgres
    password: "Han123..."
  data:
    redis:
      host: localhost
      port: 6379
      password:
      database: 0

aliyun:
  oss:
    enabled: true
    region: cn-hangzhou
    endpoint: oss-cn-hangzhou.aliyuncs.com
    bucket: hanfeihutemuimage
    # Leave empty to use default OSS public domain:
    # https://<bucket>.<endpoint>/<key>
    public-domain: ""
    # Fill locally via application-local.yml or env vars
    access-key-id: "${OSS_ACCESS_KEY_ID:LTAI5tDfNGajfkttFe3uvN1g}"
    access-key-secret: "${OSS_ACCESS_KEY_SECRET:Hobr0GBQg02mERUxyT8RJ8Ff919nCE}"

# 如果你希望提示词优化也走本地配置，可在此覆盖：
# ai:
#   image:
#     prompt-optimizer:
#       api-key: ""
# Global thread pool
thread-pool:
  workers: 4
temu:
  auto-publish:
    worker:
      enabled: true
      poll-ms: 300000
