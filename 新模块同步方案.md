# TEMU 商品同步方案

> 对应需求文档：新模块需求整理.md  
> 对应数据库设计：新模块数据库设计.md  
> 目标：支持多店铺、高并发、可恢复、可观测的 TEMU 数据同步

---

## 一、方案结论

本方案采用“任务编排 + Redis 暂存区 + PostgreSQL 批量入库”的三层同步模型，满足以下目标：

- 支持前端在同步配置页按店铺选择参与同步的对象。
- 下载阶段和入库阶段解耦，避免“同步到一半失败只能全部重来”。
- 下载完成前不写业务正式表，避免脏数据污染。
- 下载完成后统一进入 Redis 暂存区，再批量写入 PostgreSQL。
- 前端可实时看到任务进度、阶段、失败页、失败批次和可重试状态。

不建议把“全部数据先放 JVM 内存，再一次性处理”作为唯一实现方式。原因很直接：单店铺数据量大时，堆内存风险高，任务失败后也不利于恢复。更稳妥的实现是：

- 逻辑上仍然保持“全部下载完成后再入库”。
- 实现上按页下载后立即写入 Redis 暂存，而不是长期堆在 Java 内存里。
- 只有正在处理的页或批次在内存中短暂存在。

这比纯内存方案更适合大店铺和长时间任务。

---

## 二、与现有平台配置的衔接

### 2.1 当前系统里已有的凭证关系

现有后端已经形成了这条凭证解析链路：

`platform/config profile` -> `temu.shopRefId` -> `temu_shops` -> `temu_self_apps`

也就是说：

- `platform_config_profiles` / `platform_config_items` 负责维护一套平台配置。
- 其中配置项 `temu.shopRefId` 指向 `temu_shops.id`。
- `temu_shops` 中保存店铺 `shopId`、`token`。
- `temu_shops.app_id` 再关联 `temu_self_apps`，拿到 `appKey`、`appSecret`。

当前默认凭证读取逻辑已经在后端存在，可直接复用。

### 2.2 同步配置页如何选店

用户希望“在前端选择 platform/config 的数据，就能自动关联找到所有凭证”，这个理解是对的，因此同步配置页建议按下面方式设计：

- 主选择维度显示“平台配置方案”。
- 每个配置方案展示：配置名称、关联店铺名称、shopId、应用名称、是否默认。
- 保存时实际落库两类信息：
  - 选中的 `profileId`
  - 解析后的 `shop_id`

这样做的原因：

- 前端操作习惯符合你现在的配置方式。
- 后端真正执行同步时仍然使用 `shop_id` 做隔离和幂等。
- 即使后续 profile 名称变更，任务执行仍然可追溯到具体店铺。

### 2.3 配置页建议字段

同步配置页面建议包括：

- 平台配置方案选择（来自 `/api/platform/config/profiles`）
- 店铺信息展示（根据 `temu.shopRefId` 自动回填，只读）
- 同步开关：商品、生命周期、价格、运费模板、仓库、调价单、核价单
- 线程数配置：商品同步线程数、状态同步线程数、价格同步线程数
- 定时任务配置：cron 表达式
- 重试策略：单页最大重试次数、单批次最大重试次数
- 失败处理策略：是否允许人工续跑、是否保留 Redis 暂存
- 价格审核阈值：自动拒绝阈值、自动通过阈值

---

## 三、总体架构

### 3.1 核心链路

```text
前端发起同步
  |
  v
同步任务服务创建任务
  |
  v
按店铺 + 同步类型加分布式锁
  |
  v
调用 TEMU API 分页下载
  |
  v
按页写入 Redis 暂存区
  |
  v
下载完成校验
  |
  +-- 失败 -> 标记 DOWNLOAD_FAILED，保留上下文，等待人工处理
  |
  v
从 Redis 读取暂存数据
  |
  v
按批转换并 UPSERT 到 PostgreSQL 正式表
  |
  +-- 某批失败 -> 标记 PERSIST_FAILED，可从失败批次继续
  |
  v
全部成功 -> 标记 SUCCEEDED，清理 Redis 暂存
```

### 3.2 为什么 Redis 适合放在中间层

Redis 在这里不只是缓存，而是同步任务的“可恢复暂存区”：

- 支持分页写入，不要求一次性全堆内存。
- 支持任务恢复，可以从已下载页、已完成批次继续。
- 支持进度统计，前端读取简单。
- 失败时可以保留现场，方便人工干预。

因此推荐用 Redis，而不是把所有中间状态只放数据库日志或 JVM 内存。

---

## 四、任务模型设计

### 4.1 任务阶段

每个同步任务拆成两个主阶段：

1. 下载阶段
2. 入库阶段

其中下载阶段要求“尽可能完整”，入库阶段要求“可中断、可恢复、可重试”。

### 4.2 任务状态机

建议状态如下：

- `PENDING`：任务已创建，等待执行
- `DOWNLOADING`：正在拉取 TEMU 数据
- `DOWNLOADED`：下载完成，等待入库
- `DOWNLOAD_FAILED`：下载失败
- `PERSISTING`：正在写 PostgreSQL
- `PERSIST_FAILED`：入库失败
- `SUCCEEDED`：执行完成
- `CANCELLED`：人工取消

### 4.3 恢复规则

- `DOWNLOAD_FAILED`：允许“继续下载未完成页”或“从头重下”两种模式。
- `PERSIST_FAILED`：允许“从失败批次继续入库”。
- `SUCCEEDED`：只允许查看日志，不允许续跑。
- 同一店铺同一同步类型同一时刻只能有一个运行中任务。

---

## 五、Redis 暂存设计

### 5.1 Key 设计

以任务 ID 为核心，建议使用如下结构：

```text
temu:sync:task:{taskId}:meta
temu:sync:task:{taskId}:pages
temu:sync:task:{taskId}:page:{pageNo}
temu:sync:task:{taskId}:staging:list
temu:sync:task:{taskId}:errors
temu:sync:task:{taskId}:progress
```

建议含义如下：

- `meta`：任务元信息，包含 shopId、syncType、profileId、状态、总数、总页数、批次数等。
- `pages`：已完成页号集合，用于断点续传。
- `page:{pageNo}`：该页原始数据，建议 gzip 压缩后存储。
- `staging:list`：待入库页号或待入库批次队列。
- `errors`：失败页、失败批次及错误详情。
- `progress`：前端实时读取的进度信息。

### 5.2 数据粒度建议

不要把整个任务的所有数据拼成一个大 JSON 再塞 Redis。应该按页或按业务块存：

- 商品列表接口：按页存
- 商品详情接口：按 `productId` 分组存
- 价格同步：按 SKU 批次存
- 生命周期：按页存

这样有几个直接好处：

- 单个 key 不会过大。
- 失败重试只需要重跑局部。
- Redis TTL 和清理策略更容易控制。

### 5.3 TTL 策略

建议：

- 运行中任务：不设置 TTL
- `DOWNLOAD_FAILED` / `PERSIST_FAILED`：保留 3 到 7 天
- `SUCCEEDED`：成功后立即清理，或最多保留 24 小时供排查

---

## 六、详细同步流程

### 6.1 步骤一：创建任务

用户在前端选择平台配置方案和同步类型后，后端执行：

1. 校验 `profileId` 是否存在
2. 读取 `temu.shopRefId`
3. 解析得到 `TemuShop`
4. 通过 `TemuShop.app` 拿到 `appKey/appSecret`
5. 创建同步任务记录
6. 获取分布式锁

锁建议使用：

```text
temu:sync:lock:{shopId}:{syncType}
```

### 6.2 步骤二：下载 TEMU 数据

下载阶段遵循“分页成功即写 Redis”的原则：

1. 请求第一页，拿到 `totalCount`
2. 计算总页数
3. 按配置线程池并发拉取其余页
4. 每页成功后立即写 Redis 暂存
5. 同时更新任务进度
6. 任意页失败时记录失败页号和错误码

这里建议加入“单页重试机制”：

- 网络错误或 `1000001`、`1000005` 这类临时错误：自动重试 3 次
- 明确参数错误：不自动重试，直接失败

### 6.3 步骤三：下载完成校验

下载完成后，必须做校验，至少包括：

- Redis 中已完成页数是否等于总页数
- 实际下载条数是否等于接口返回的 `totalCount`
- 主键是否重复，例如 `productSkcId`、`productSkuId`
- 详情类接口是否和列表主数据对齐

校验通过后，任务状态改为 `DOWNLOADED`。

### 6.4 步骤四：批量入库

入库阶段从 Redis 读取暂存数据，转换后写 PostgreSQL：

1. 从 Redis 组装当前待入库批次
2. 转换成主表和子表结构
3. 使用批量 UPSERT 写入正式表
4. 单批次成功后提交事务并记录进度
5. 某批次失败则回滚该批次，并记录 `failed_batch_no`

建议批量写库规则：

- 主表使用 `INSERT ... ON CONFLICT DO UPDATE`
- 子表按主键或唯一键先删后插，或直接 UPSERT
- 每批 100 到 500 条，按实际压测调整

### 6.5 步骤五：完成或人工干预

任务结束后的处理：

- 成功：更新状态为 `SUCCEEDED`，写完成时间，清理 Redis 暂存
- 下载失败：保留已下载页和失败页信息，支持人工点击“继续下载”
- 入库失败：保留 Redis 暂存和失败批次信息，支持点击“继续入库”

---

## 七、各同步类型的执行建议

### 7.1 商品信息同步

商品同步应拆成两层：

1. `bg.glo.goods.list.get` 拉 SKC 列表
2. 对列表中的 `productId` 再调用 `bg.glo.goods.detail.get` 拉详情

建议流程：

- 先分页拉列表并写 Redis
- 再以 `productId` 为单位补详情并回填 Redis
- 两部分都完成后再进入 PostgreSQL

### 7.2 生命周期同步

`bg.glo.product.search` 可按页直接拉取，数据量通常比商品详情轻，可单独任务运行。

### 7.3 价格同步

价格同步依赖本地已存在 SKU，因此必须加前置校验：

- 若商品主数据还未同步完成，不允许启动价格同步
- 价格接口按 SKU ID 批量调用
- 每个 SKU 批次独立写 Redis 和入库

### 7.4 运费模板、仓库、调价单、核价单

这几类数据量通常较小，但仍建议走同一套任务框架，原因是：

- 日志和进度呈现统一
- 失败恢复统一
- 后续扩展成本低

---

## 八、前端页面方案

### 8.1 页面拆分建议

建议前端增加三个页面或区域：

1. 同步配置页
2. 同步任务列表页
3. 同步任务详情抽屉/弹窗

### 8.2 同步配置页

页面主要能力：

- 选择平台配置方案
- 自动展示关联店铺与应用信息
- 维护线程数、cron、阈值、失败重试策略
- 选择默认同步类型
- 一键保存并启停调度

建议列表字段：

- 配置方案名称
- 关联店铺名称
- shopId
- appName
- 是否启用
- 商品线程数
- 生命周期线程数
- 价格线程数
- cron
- 最近执行时间

### 8.3 同步任务列表页

建议展示：

- 任务 ID
- 店铺名称
- 同步类型
- 当前阶段
- 当前状态
- 下载进度
- 入库进度
- 开始时间
- 最近更新时间
- 触发方式（手动/定时）
- 操作按钮

操作按钮建议：

- 查看详情
- 继续下载
- 继续入库
- 从头重跑
- 取消任务

### 8.4 任务详情页

建议展示内容：

- 基本信息：店铺、配置方案、同步类型、任务状态
- 下载信息：总页数、完成页数、失败页列表
- 入库信息：总批次、完成批次、失败批次
- 错误信息：错误码、错误文案、最后失败时间
- 时间轴日志：按时间顺序展示关键事件

### 8.5 实时进度推送方式

建议优先使用 SSE，不优先用 WebSocket。

原因：

- 这里主要是服务端单向推送进度。
- SSE 实现更轻，和当前后台管理场景更匹配。
- 断线自动重连能力足够。

接口建议：

- 查询任务列表：普通 HTTP
- 查询任务详情：普通 HTTP
- 查询实时进度：SSE

如果当前前端接入 SSE 成本高，可以先做 3 秒轮询，后面再升级 SSE。

---

## 九、数据库落地建议

现有文档里已经有：

- `temu_sync_config`
- `temu_sync_log`

但如果要真正支持“下载到一半失败后继续”“入库失败后从批次继续”，这两个表还不够。建议新增以下表。

### 9.1 同步任务主表 `temu_sync_task`

建议核心字段：

- `id`
- `profile_id`
- `shop_id`
- `sync_type`
- `trigger_type`
- `status`
- `download_total`
- `download_completed`
- `download_failed`
- `persist_total`
- `persist_completed`
- `persist_failed`
- `current_phase`
- `failed_page_nos_json`
- `failed_batch_nos_json`
- `last_error_code`
- `last_error_msg`
- `redis_snapshot_key`
- `started_at`
- `finished_at`
- `created_at`
- `updated_at`

### 9.2 同步任务明细表 `temu_sync_task_detail`

建议用于记录页或批次级别执行结果，核心字段：

- `id`
- `task_id`
- `phase`（DOWNLOAD / PERSIST）
- `detail_type`（PAGE / BATCH / ITEM）
- `detail_no`
- `status`
- `record_count`
- `error_code`
- `error_msg`
- `payload_ref`
- `started_at`
- `finished_at`

### 9.3 现有表如何继续使用

- `temu_sync_config` 继续保留，负责每店铺同步配置。
- `temu_sync_log` 可以保留作为轻量操作日志，或转为任务汇总日志。
- 任务恢复和进度展示以 `temu_sync_task` / `temu_sync_task_detail` 为主。

---

## 十、接口建议

### 10.1 配置接口

- `GET /api/platform/sync/configs`
- `POST /api/platform/sync/configs`
- `PUT /api/platform/sync/configs/{id}`
- `GET /api/platform/sync/configs/options/profiles`

其中 `options/profiles` 返回 platform/config 方案及其解析后的店铺、应用信息。

### 10.2 任务接口

- `POST /api/platform/sync/tasks` 创建任务
- `GET /api/platform/sync/tasks` 查询任务列表
- `GET /api/platform/sync/tasks/{taskId}` 查询任务详情
- `POST /api/platform/sync/tasks/{taskId}/resume-download` 继续下载
- `POST /api/platform/sync/tasks/{taskId}/resume-persist` 继续入库
- `POST /api/platform/sync/tasks/{taskId}/rerun` 从头重跑
- `POST /api/platform/sync/tasks/{taskId}/cancel` 取消任务
- `GET /api/platform/sync/tasks/{taskId}/events` SSE 推送

---

## 十一、异常处理策略

### 11.1 自动重试场景

适合自动重试的情况：

- 网络超时
- TEMU 临时性服务异常
- Redis 短暂抖动

不适合自动重试的情况：

- 参数错误
- 店铺凭证失效
- 配置缺失
- 类目/业务数据不合法

### 11.2 人工干预场景

以下情况应支持人工处理：

- 某页反复失败，但其他页已成功
- PostgreSQL 某批次数据转换失败
- 店铺 token 过期，需要重新配置后继续
- 某次任务不想重头跑，只想从失败点续跑

### 11.3 幂等要求

所有正式业务表继续依赖唯一键 + UPSERT，确保：

- 同一个任务重复入库不会插入重复数据
- 断点续跑不会导致数据翻倍
- 从失败批次继续不会破坏正式数据

---

## 十二、实施优先级建议

建议分三期做，避免一口气做得过重。

### 第一期：先跑通主链路

- 同步配置页
- 商品信息同步任务
- Redis 暂存
- PostgreSQL 批量入库
- 任务列表页
- 失败后从批次继续入库

### 第二期：补齐可恢复能力

- 下载失败后继续下载未完成页
- SSE 实时进度
- 页级和批次级明细日志
- 运费模板、仓库、生命周期统一纳入框架

### 第三期：完善调度和治理

- 定时任务调度
- 限流与熔断
- 告警通知
- 自动清理 Redis 暂存
- 压测与线程数自动调优

---

## 十三、最终推荐

结合你提出的要求，最终推荐方案如下：

- 前端按 platform/config 方案选店，不直接让用户拼凭证。
- 同步任务按店铺和同步类型拆分，任务状态独立管理。
- 下载阶段分页拉取后立即写 Redis，不长期堆 JVM 内存。
- 所有页下载完成并校验成功后，再批量写 PostgreSQL。
- 任意阶段失败都保留任务上下文，支持人工续跑。
- 前端任务页实时展示下载进度、入库进度、失败页、失败批次。

这个方案比“全部先塞内存再处理”更稳，也比“边拉边落正式库”更安全，适合你现在这个多店铺、数据量大、需要人工干预的场景。

### 7.3 任务详情 / 日志弹窗

```
┌────────────────────────────────────────────────────────────┐
│  同步任务 #42 — 商品信息 — 店铺A                    [关闭] │
├────────────────────────────────────────────────────────────┤
│  状态: ● 入库失败         触发: 手动                       │
│  下载: 1523/1523 条 ✓     入库: 800/1523 条 (批次 4/8)    │
│  下载耗时: 12.3s           入库耗时: 进行中                │
│  重试次数: 0                                               │
│                                        [重试入库] [全量重试] │
├────────────────────────────────────────────────────────────┤
│  执行日志                          筛选: [全部级别 ▼]       │
│                                                            │
│  10:23:45  INFO   [下载] 开始下载，共 1523 条，16 页       │
│  10:23:46  INFO   [下载] 第 1/16 页成功，100 条，320ms     │
│  10:23:46  INFO   [下载] 第 2/16 页成功，100 条，285ms     │
│  ...                                                       │
│  10:23:58  INFO   [下载] 下载完成，快照已保存 (4.2MB→680KB)│
│  10:23:58  INFO   [入库] 开始入库，8 批次                  │
│  10:23:59  INFO   [入库] 批次 1/8 成功，200 条，156ms      │
│  ...                                                       │
│  10:24:01  ERROR  [入库] 批次 5/8 失败                     │
│                   └─ duplicate key: shop1_skc_123          │
│  10:24:01  ERROR  [入库] 入库终止，等待重试                │
└────────────────────────────────────────────────────────────┘
```

### 7.4 新建同步弹窗

```
┌──────────────────────────────────────────┐
│  发起数据同步                    [关闭] │
├──────────────────────────────────────────┤
│                                          │
│  店铺:     [店铺A             ▼]         │
│                                          │
│  同步类型: ☑ 商品信息                    │
│           ☑ 商品状态                     │
│           ☑ 供货价                       │
│           ☐ 调价单                       │
│           ☐ 核价单                       │
│           ☐ 运费模板                     │
│           ☐ 发货仓库                     │
│           ☐ 活动                         │
│                                          │
│  ⓘ 供货价同步依赖商品信息数据，         │
│    系统会自动按顺序执行                   │
│                                          │
│                    [取消]  [开始同步]     │
└──────────────────────────────────────────┘
```

### 7.5 前端轮询机制

```
前端每 3 秒轮询一次（任务进行中时）:

GET /api/temu-sync/tasks/{taskId}/progress

返回:
{
  "status": "DOWNLOADING",
  "totalCount": 1523,
  "downloadedCount": 800,
  "totalPages": 16,
  "downloadedPages": 8,
  "persistedCount": 0,
  "totalBatches": 0,
  "persistedBatches": 0,
  "latestLogs": [            // 最近 5 条日志
    { "level": "INFO", "phase": "DOWNLOAD", "message": "第 8/16 页成功" }
  ]
}

任务结束后自动停止轮询。
```

---

## 八、后端 API 设计

### 8.1 接口列表

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/temu-sync/tasks` | 创建同步任务（支持多类型批量提交） |
| GET | `/api/temu-sync/tasks` | 查询任务列表（分页、筛选） |
| GET | `/api/temu-sync/tasks/{id}` | 查询任务详情 |
| GET | `/api/temu-sync/tasks/{id}/progress` | 查询任务实时进度（轮询用） |
| GET | `/api/temu-sync/tasks/{id}/logs` | 查询任务步骤日志（分页） |
| POST | `/api/temu-sync/tasks/{id}/retry` | 重试入库（仅 PERSIST_FAILED 状态可用） |
| POST | `/api/temu-sync/tasks/{id}/cancel` | 取消任务（仅 CREATED / DOWNLOADED 状态） |

### 8.2 创建同步任务

```
POST /api/temu-sync/tasks
{
  "shopId": "634418212966313",
  "syncTypes": ["GOODS", "LIFECYCLE", "PRICE"]
}

返回:
{
  "tasks": [
    { "id": 42, "syncType": "GOODS",     "status": "CREATED" },
    { "id": 43, "syncType": "LIFECYCLE",  "status": "CREATED" },
    { "id": 44, "syncType": "PRICE",      "status": "CREATED", "dependsOn": 42 }
  ]
}
```

### 8.3 重试入库

```
POST /api/temu-sync/tasks/42/retry
{
  "mode": "CONTINUE"   // CONTINUE = 从失败批次继续; FULL = 全量重新入库
}

返回:
{
  "id": 42,
  "status": "PERSISTING",
  "retryCount": 1,
  "message": "从批次 5/8 继续入库"
}
```

---

## 九、定时同步与依赖编排

### 9.1 每日定时同步

```
┌──────────────────────────────────────────────────────┐
│  每日定时同步 (cron 配置)                             │
│                                                      │
│  Phase A: 并行执行（无依赖）                          │
│  ┌──────────┐  ┌───────────┐  ┌──────────┐          │
│  │ 商品信息  │  │ 商品状态   │  │ 运费模板  │          │
│  │  GOODS   │  │ LIFECYCLE │  │ FREIGHT  │          │
│  └─────┬────┘  └───────────┘  └──────────┘          │
│        │                                             │
│  Phase B: 依赖 GOODS 完成                             │
│        ▼                                             │
│  ┌──────────┐                                        │
│  │ 供货价    │                                        │
│  │  PRICE   │                                        │
│  └──────────┘                                        │
│                                                      │
│  Phase C: 独立执行                                    │
│  ┌──────────┐  ┌───────────┐  ┌──────────┐          │
│  │ 调价单    │  │ 核价单     │  │ 活动      │          │
│  │P_ADJUST  │  │ P_REVIEW  │  │ ACTIVITY │          │
│  └──────────┘  └───────────┘  └──────────┘          │
└──────────────────────────────────────────────────────┘
```

### 9.2 依赖规则

| 同步类型 | 前置依赖 | 说明 |
|---------|---------|------|
| GOODS | 无 | 可独立执行 |
| LIFECYCLE | 无 | 可独立执行 |
| PRICE | GOODS（当次或历史） | 需要 SKU ID 数据 |
| FREIGHT | 无 | 可独立执行 |
| WAREHOUSE | 无 | 可独立执行 |
| PRICE_ADJUST | 无 | 可独立执行 |
| PRICE_REVIEW | 无 | 可独立执行 |
| ACTIVITY | 无 | 可独立执行 |

### 9.3 定时任务失败处理

- 定时同步中如果某个类型失败，**不影响其他类型**的同步
- 失败任务会在前端同步任务列表中展示，管理员可查看日志并决定是否手动重试
- 连续 3 次定时同步失败时，可考虑发送告警通知（预留扩展）

---

## 十、核心代码架构

### 10.1 类图概览

```
SyncTaskService                       // 任务管理（创建、查询、重试、取消）
 │
 ├── SyncExecutorFactory              // 根据 syncType 创建对应 Executor
 │    ├── GoodsSyncExecutor           // 商品信息同步
 │    ├── LifecycleSyncExecutor       // 商品状态同步
 │    ├── PriceSyncExecutor           // 供货价同步
 │    ├── FreightSyncExecutor         // 运费模板同步
 │    ├── WarehouseSyncExecutor       // 发货仓库同步
 │    ├── PriceAdjustSyncExecutor     // 调价单同步
 │    ├── PriceReviewSyncExecutor     // 核价单同步
 │    └── ActivitySyncExecutor        // 活动同步
 │
 ├── AbstractSyncExecutor<T>          // 抽象基类，封装两阶段通用逻辑
 │    ├── downloadPhase()             // Phase 1: API 拉取 → 内存
 │    ├── persistPhase()              // Phase 2: 内存 → DB
 │    ├── saveSnapshot()              // 序列化 + 压缩 → DB
 │    ├── loadSnapshot()              // DB → 解压 + 反序列化
 │    └── logStep()                   // 写步骤日志
 │
 ├── SyncScheduler                    // 定时调度
 │    └── dailySync()                 // @Scheduled，按依赖编排
 │
 └── TemuApiClient                    // TEMU API 调用封装
      ├── callWithRetry()             // 内置重试（API 层面的瞬时错误）
      └── rateLimiter                 // QPS 限流
```

### 10.2 AbstractSyncExecutor 核心伪代码

```java
public abstract class AbstractSyncExecutor<T> {

    /** 子类实现：调用 TEMU API 分页拉取所有数据 */
    protected abstract List<T> doDownload(SyncTask task, SyncConfig config) throws SyncException;

    /** 子类实现：将数据转换为实体并批量入库 */
    protected abstract void doPersistBatch(SyncTask task, List<T> batch, int batchIndex);

    // ─── Phase 1: 下载 ───
    public void executeDownload(SyncTask task) {
        try {
            updateStatus(task, DOWNLOADING);
            logStep(task, DOWNLOAD, INFO, "开始下载");

            List<T> allData = doDownload(task, loadConfig(task.getShopId()));

            // 校验
            if (allData.size() != task.getTotalCount()) {
                logStep(task, DOWNLOAD, WARN,
                    format("数量不一致: 下载 %d 条, API 声明 %d 条",
                        allData.size(), task.getTotalCount()));
            }

            // 保存快照
            saveSnapshot(task, allData);
            logStep(task, DOWNLOAD, INFO,
                format("下载完成, %d 条, 快照 %s", allData.size(), formatSize(task.getSnapshotSize())));

            updateStatus(task, DOWNLOADED);

            // 自动进入入库阶段
            executePersist(task, allData);

        } catch (Exception e) {
            logStep(task, DOWNLOAD, ERROR, "下载失败: " + e.getMessage());
            updateStatus(task, DOWNLOAD_FAILED);
            task.setErrorSummary(e.getMessage());
        }
    }

    // ─── Phase 2: 入库 ───
    public void executePersist(SyncTask task, List<T> allData) {
        try {
            // 如果是重试，从快照恢复
            if (allData == null) {
                allData = loadSnapshot(task);
            }

            updateStatus(task, PERSISTING);

            int batchSize = 200;
            List<List<T>> batches = partition(allData, batchSize);
            task.setTotalBatches(batches.size());

            int startBatch = task.getPersistedBatches(); // 重试时跳过已完成的批次

            for (int i = startBatch; i < batches.size(); i++) {
                try {
                    doPersistBatch(task, batches.get(i), i);

                    task.setPersistedBatches(i + 1);
                    task.setPersistedCount(Math.min((i + 1) * batchSize, allData.size()));
                    logStep(task, PERSIST, INFO,
                        format("批次 %d/%d 成功, %d 条", i + 1, batches.size(), batches.get(i).size()));

                } catch (Exception e) {
                    task.setFailedBatchIndex(i);
                    logStep(task, PERSIST, ERROR,
                        format("批次 %d/%d 失败: %s", i + 1, batches.size(), e.getMessage()));
                    updateStatus(task, PERSIST_FAILED);
                    task.setErrorSummary(e.getMessage());
                    return;
                }
            }

            // 全部成功
            task.setSnapshotData(null); // 清理快照
            updateStatus(task, SUCCEEDED);
            logStep(task, PERSIST, INFO,
                format("入库完成, 共 %d 条", allData.size()));

        } catch (Exception e) {
            logStep(task, PERSIST, ERROR, "入库异常: " + e.getMessage());
            updateStatus(task, PERSIST_FAILED);
        }
    }

    // ─── 重试 ───
    public void retryPersist(SyncTask task, String mode) {
        if (task.getStatus() != PERSIST_FAILED) {
            throw new IllegalStateException("仅入库失败状态可重试");
        }
        task.setRetryCount(task.getRetryCount() + 1);
        if ("FULL".equals(mode)) {
            task.setPersistedBatches(0);
            task.setPersistedCount(0);
        }
        executePersist(task, null); // null → 从快照恢复
    }
}
```

---

## 十一、异常场景汇总

| 场景 | 处理方式 | 最终状态 |
|------|---------|---------|
| API 返回 errorCode != 0 | 记录错误日志，终止下载 | DOWNLOAD_FAILED |
| API 网络超时 | 内置 3 次重试，仍失败则终止 | DOWNLOAD_FAILED |
| API 返回空数据（totalCount=0） | 正常完成，记录警告日志 | SUCCEEDED（0 条入库） |
| 下载数量与 totalCount 不一致 | 记录警告日志，继续入库（以实际下载数为准） | SUCCEEDED / PERSIST_FAILED |
| 入库唯一键冲突 | UPSERT 机制自动处理，正常不应报错 | SUCCEEDED |
| 入库事务超时 | 记录失败批次，等待重试 | PERSIST_FAILED |
| 入库数据格式异常 | 记录失败批次 + detail_json 包含异常数据 | PERSIST_FAILED |
| 快照数据损坏（解压失败） | 记录错误，需重新发起同步 | 需人工处理 |
| 应用重启（任务执行中） | 定时扫描 DOWNLOADING/PERSISTING 超过 30 分钟的任务，标记为失败 | DOWNLOAD_FAILED / PERSIST_FAILED |
| 同一店铺同类型重复提交 | 部分唯一索引拦截，返回 409 Conflict | — |

---

## 十二、数据完整性保证

### 12.1 下载阶段完整性

```
检查项:
  ✓ 下载条数 == API totalCount — 防止分页遗漏
  ✓ 每页数据校验 — success == true，非空
  ✓ fail-fast — 任一页失败立即停止，不产生不完整数据
  ✓ 快照持久化 — 下载数据不仅在内存，还压缩存 DB
```

### 12.2 入库阶段完整性

```
检查项:
  ✓ 每批独立事务 — 批次失败自动 ROLLBACK，不影响已入库批次
  ✓ UPSERT 幂等 — ON CONFLICT DO UPDATE，重复执行结果一致
  ✓ 子表先删后插 — 防止旧数据残留（如 SKU 规格变更）
  ✓ 重试可断点续传 — 记录 persisted_batches，从失败处继续
  ✓ 全量重试选项 — 如果怀疑数据问题，可选择全量重新入库
```

### 12.3 数据一致性窗口

同步过程中，新旧数据可能短暂共存（部分批次已更新，部分未更新）。这对于本系统是可接受的：

- 商品查询页面展示的是 **快照时间点** 的数据
- 每条记录有 `synced_at` 字段标识同步时间
- 如有强一致性需求，可在入库前先对整个 shop 的数据加版本号

---

## 总结

| 设计目标 | 解决方案 |
|---------|---------|
| 数据完整性 | 两阶段模型；下载阶段 all-or-nothing |
| 数据准确性 | totalCount 校验；UPSERT 幂等；raw_json 全量备份 |
| 性能 | 多线程下载；批量入库；快照压缩 |
| 可追溯性 | temu_sync_step_log 记录每一步日志；前端实时查看 |
| 容错性 | 入库失败可重试（断点续传或全量）；快照不丢失 |
| 前端可见 | 任务列表 + 实时进度 + 日志弹窗 + 重试按钮 |
