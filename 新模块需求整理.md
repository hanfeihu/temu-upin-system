# 新模块需求整理

> 说明：本文件由原始网页复制内容整理而成，请求参数与返回参数改为树形层级展示，层级依据原始 HTML 的 `data-row-key` 树结构提取，尽量避免字段归属错误。

## 一、业务需求摘要

- 建立新的模块 和同步数据机制因为数据量比较大如果下来下来的话多线程配置 数据存储一定要谨慎珍惜每一个字段都要存 前端搜索条件要完善 分主要的 和更多 或者高级搜索 可以通过价格搜索
- 需要有一个模块来配置同步机制的配置当然有默认配置，比如商品同步 商品状态同步线程数页在这里配置 每天都要同步一次 商品的状态 和商品信息 因为不确定有没有新增 和状态变更 所有每天都要同步一次
- 因为价格接口 产品信息接口  产品状体接口都是不一样的接口  并且需要有一个核价模块  在上面那个配置里面 还需要有个配置是低于多少钱自动拒绝 比如低于28块钱 核价自动拒绝  还有一个高于多少自动同意  同步过来的数据都要有店铺ID存储的时候哦 因为sdk 查询的时候都要给凭证嘛 凭证上就有店铺ID 了 ；商品模块 可以根据状态 和 价格查询
- 最终要实现的是通过系统可以看到商品商品状态详情活动活动价格 申报价格 能根据价格查询 搜索 也能根据活动价格查询搜索 还有能根据类目查询搜索

## 二、接口文档

### 1. bg.glo.goods.list.get - 商品列表查询

- 更新时间：2025-08-27 16:57:43
- 接口介绍：货品skc列表查询

#### 请求地址
| 调用地址/地区 | 数据存储 |
| --- | --- |
| /openapi/router | CN |

#### 公共请求参数
| 参数接口 | 参数类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| type | STRING | 是 | API接口名, 形如:bg.* |
| app_key | STRING | 是 | 已创建成功的应用标志 |
| timestamp | STRING | 是 | 时间戳，格式为UNIX时间（秒） ，长度10位，当前时间-300秒<=入参时间<=当前时间+300秒 |
| sign | STRING | 是 | API入参参数签名，签名值根据如下算法给出计算过程 |
| data_type | STRING | 否 | 请求返回的数据格式，可选参数固定为JSON |
| access_token | STRING | 是 | 用户授权令牌access_token，卖家中心—授权管理，申请授权生成 |
| version | STRING | 是 | API版本，默认为V1，无要求不传此参数 |

#### 请求参数说明（树形层级）
- `createdAtStart`: INTEGER，必填：否，说明：Creation Time Start
- `cat8Id`: INTEGER，必填：否，说明：8th Level Category ID
- `skcExtCode`: STRING，必填：否，说明：Product SKC External Code
- `cat9Id`: INTEGER，必填：否，说明：9th Level Category ID
- `cat3Id`: INTEGER，必填：否，说明：Third-Level Category ID
- `cat4Id`: INTEGER，必填：否，说明：Fourth-Level Category ID
- `cat5Id`: INTEGER，必填：否，说明：Fifth-Level Category ID
- `cat6Id`: INTEGER，必填：否，说明：Sixth-Level Category ID
- `pageSize`: INTEGER，必填：否，说明：Page Size
- `cat1Id`: INTEGER，必填：否，说明：First-Level Category ID
- `cat2Id`: INTEGER，必填：否，说明：Secondary Category ID
- `productName`: STRING，必填：否，说明：Product Name
- `isSupportPersonalization`: BOOLEAN，必填：否，说明：Whether to Support Customized Products
- `productSkcIds`: LIST，必填：否，说明：Product SKC ID List
  - `$item`: INTEGER，必填：否，说明：-
- `quickSellAgtSignStatus`: INTEGER，必填：否，说明：Quick Sales Agreement Signing Status 0-Unsigned 1-Signed
- `matchJitMode`: BOOLEAN，必填：否，说明：Whether to hit JIT mode
- `cat10Id`: INTEGER，必填：否，说明：Tenth-Level Category ID
- `bindSiteIds`: LIST，必填：否，说明：Operational Site ID List
  - `$item`: INTEGER，必填：否，说明：-
- `skcSiteStatus`: INTEGER，必填：否，说明：SKC Site Status
- `page`: INTEGER，必填：否，说明：Page Number
- `createdAtEnd`: INTEGER，必填：否，说明：Creation Time End

#### 返回参数说明（树形层级）
  - `data`: LIST，说明：Result List
    - `$item`: OBJECT，说明：-
      - `productProperties`: LIST，说明：Product General Attributes
        - `$item`: OBJECT，说明：-
          - `vid`: INTEGER，说明：Basic Attribute Value ID
          - `valueUnit`: STRING，说明：Attribute Value Unit
          - `language`: STRING，说明：Language
          - `pid`: INTEGER，说明：Attribute ID
          - `templatePid`: INTEGER，说明：Template Attribute ID
          - `numberInputValue`: STRING，说明：Numerical Input
          - `propValue`: STRING，说明：Basic Property Value
          - `propName`: STRING，说明：Reference Property Name
          - `valueExtendInfo`: STRING，说明：Attribute Value Extension Information
          - `refPid`: INTEGER，说明：Reference Property ID
      - `productId`: INTEGER，说明：Product Id
      - `productJitMode`: OBJECT，说明：Product JIT Mode Information
        - `signLatestJitVersion`: BOOLEAN，说明：Whether to sign the latest version of JIT pre-sale agreement
        - `quickSellAgtSignStatus`: INTEGER，说明：Quick Sales Agreement Signing Status 0-Unsigned 1-Signed
        - `matchJitMode`: BOOLEAN，说明：Whether JIT Mode
      - `productSkuSummaries`: LIST，说明：SKU Summary Information List
        - `$item`: OBJECT，说明：-
          - `productSkuId`: INTEGER，说明：Product SKU ID
          - `extCode`: STRING，说明：SKU Number
          - `productSkuWhExtAttr`: OBJECT，说明：Product SKU Warehouse Extension Attributes
            - `productSkuWeight`: OBJECT，说明：Weight
              - `value`: INTEGER，说明：Weight Value (Milligrams)
            - `productSkuWmsVolume`: OBJECT，说明：WMS Volume
              - `len`: INTEGER，说明：Length (mm)
              - `wmsCollectionSourceType`: INTEGER，说明：WMS Collection Source (inconsistent with WMS enum, only record sources of interest)
              - `width`: INTEGER，说明：Width (mm)
              - `height`: INTEGER，说明：High (mm)
            - `productSkuBarCodes`: LIST，说明：Barcode
              - `$item`: OBJECT，说明：-
                - `code`: STRING，说明：Barcode
                - `codeType`: INTEGER，说明：Barcode Type (1: EAN, 2: UPC, 3: ISBN)
            - `productSkuSubSellMode`: INTEGER，说明：Sub-Sales Mode
            - `productSkuSensitiveAttr`: OBJECT，说明：Sensitive Attribute
              - `sensitiveTypes`: LIST，说明：Sensitive Type PURE_ELECTRIC(1, "Pure Electric"), INTERNAL_ELECTRIC(2, "Internal Electric"), MAGNETISM(3, "Magnetism"), LIQUID(4, "Liquid"), POWDER(5, "Powder"), PASTE(6, "Paste"), CUTTER(7, "Tool")
                - `$item`: INTEGER，说明：-
              - `isSensitive`: INTEGER，说明：Whether Sensitive Attribute, 0: Non-Sensitive, 1: Sensitive
            - `productSkuFragileLabels`: OBJECT，说明：Fragile Label
              - `isFragile`: BOOLEAN，说明：Whether Fragile Item
            - `productSkuNewSensitiveAttr`: OBJECT，说明：Sensitive Attribute
              - `force2NormalTypes`: LIST，说明：Force Conversion to General Type
                - `$item`: INTEGER，说明：-
              - `sensitiveList`: LIST，说明：Sensitive Product Type
                - `$item`: INTEGER，说明：-
              - `isForce2Normal`: BOOLEAN，说明：Whether to Forcefully Convert to General Goods
            - `productSkuVolumeLabel`: OBJECT，说明：Volume Label
              - `isSideOverLength`: BOOLEAN，说明：Whether Exceeding Length Limit
              - `isVolumeOverSize`: BOOLEAN，说明：Whether Extremely Large Volume
            - `productSkuWmsWeight`: OBJECT，说明：WMS Weight
              - `wmsCollectionSourceType`: INTEGER，说明：WMS Collection Source (inconsistent with WMS enum, only record sources of interest)
              - `value`: INTEGER，说明：Weight Value (Milligrams)
            - `productSkuVolume`: OBJECT，说明：Volume
              - `len`: INTEGER，说明：Length (mm)
              - `width`: INTEGER，说明：Width (mm)
              - `height`: INTEGER，说明：High (mm)
            - `productSkuSensitiveLimit`: OBJECT，说明：Sensitive Property Restriction
              - `maxBatteryCapacityHp`: INTEGER，说明：Maximum Battery Capacity (mWh)
              - `maxBatteryCapacity`: INTEGER，说明：Maximum Battery Capacity (Wh)
              - `maxLiquidCapacity`: INTEGER，说明：Maximum Liquid Capacity (mL)
              - `maxLiquidCapacityHp`: INTEGER，说明：Maximum Liquid Capacity (μL)
              - `maxKnifeLength`: INTEGER，说明：Maximum Tool Length (mm)
              - `maxKnifeLengthHp`: INTEGER，说明：Maximum Tool Length (μm)
            - `productSkuWmsVolumeLabel`: OBJECT，说明：WMS Volume Label
              - `isSideOverLength`: BOOLEAN，说明：Whether Exceeding Length Limit
              - `isVolumeOverSize`: BOOLEAN，说明：Whether Extremely Large Volume
          - `virtualStock`: INTEGER，说明：Virtual Inventory
          - `productSkuSpecList`: LIST，说明：Specification List
            - `$item`: OBJECT，说明：-
              - `specId`: INTEGER，说明：Specification ID
              - `parentSpecName`: STRING，说明：Parent Specification Name
              - `parentSpecId`: INTEGER，说明：Parent Specification ID
              - `specName`: STRING，说明：Specification Name
          - `productSkuSaleExtAttr`: OBJECT，说明：Product SKU Sales Domain Extension Attributes
            - `productSkuShippingMode`: INTEGER，说明：Product SKU Shipping Mode
            - `productSkuIndividuallyPacked`: INTEGER，说明：Whether the Product SKU is Independently Packaged
      - `productName`: STRING，说明：Product Name
      - `createdAt`: INTEGER，说明：Listing Time
      - `productSemiManaged`: OBJECT，说明：Semi-managed Merchant Goods Information
        - `productShipment`: OBJECT，说明：Product Delivery Information
          - `freightTemplateId`: STRING，说明：Shipping Template ID
          - `shipmentLimitSecond`: INTEGER，说明：Guaranteed Shipping Time (unit: s)
          - `warehouseRegionId1List`: LIST，说明：First-level Regional ID List for Shipping Warehouses
            - `$item`: INTEGER，说明：-
          - `longTransport`: BOOLEAN，说明：Long-term transportation template, null represent false
        - `bindSites`: LIST，说明：Bound Site List
          - `$item`: OBJECT，说明：-
            - `siteId`: INTEGER，说明：Site ID
            - `siteName`: STRING，说明：Site Name
      - `isSupportPersonalization`: BOOLEAN，说明：Whether to Support Customized Products
      - `extCode`: STRING，说明：Product SKC External Code
      - `leafCat`: OBJECT，说明：Leaf Category
        - `catId`: INTEGER，说明：Category ID
        - `catName`: STRING，说明：Category Name
      - `skcSiteStatus`: INTEGER，说明：SKC Site Status
      - `categories`: OBJECT，说明：Category
        - `cat4`: OBJECT，说明：Fourth-Level Category
          - `catId`: INTEGER，说明：Category ID
          - `catName`: STRING，说明：Category Name
        - `cat5`: OBJECT，说明：Fifth-Level Category
          - `catId`: INTEGER，说明：Category ID
          - `catName`: STRING，说明：Category Name
        - `cat2`: OBJECT，说明：Secondary Category
          - `catId`: INTEGER，说明：Category ID
          - `catName`: STRING，说明：Category Name
        - `cat3`: OBJECT，说明：Third-Level Category
          - `catId`: INTEGER，说明：Category ID
          - `catName`: STRING，说明：Category Name
        - `cat1`: OBJECT，说明：First-Level Category
          - `catId`: INTEGER，说明：Category ID
          - `catName`: STRING，说明：Category Name
        - `cat10`: OBJECT，说明：Tenth-Level Category
          - `catId`: INTEGER，说明：Category ID
          - `catName`: STRING，说明：Category Name
        - `leafCat`: OBJECT，说明：Leaf Category
          - `catId`: INTEGER，说明：Category ID
          - `catName`: STRING，说明：Category Name
        - `cat8`: OBJECT，说明：8th Level Category
          - `catId`: INTEGER，说明：Category ID
          - `catName`: STRING，说明：Category Name
        - `cat9`: OBJECT，说明：Ninth-Level Category
          - `catId`: INTEGER，说明：Category ID
          - `catName`: STRING，说明：Category Name
        - `cat6`: OBJECT，说明：Sixth-Level Category
          - `catId`: INTEGER，说明：Category ID
          - `catName`: STRING，说明：Category Name
        - `cat7`: OBJECT，说明：Seventh-Level Category
          - `catId`: INTEGER，说明：Category ID
          - `catName`: STRING，说明：Category Name
      - `productSkcId`: INTEGER，说明：Product SKC ID
      - `mainImageUrl`: STRING，说明：SKC Main Image
      - `matchSkcJitMode`: BOOLEAN，说明：Whether to hit SKC layer JIT mode
  - `totalCount`: INTEGER，说明：Total Count
- `success`: BOOLEAN，说明：status
- `errorCode`: INTEGER，说明：error code
- `errorMsg`: STRING，说明：error message

#### 返回错误码说明
| 错误码 | 错误描述 | 解决办法 |
| --- | --- | --- |
| 1000001 | 服务器开小差 | 一般是系统抖动，可参考具体报错文案尝试解决或重试，如果还不行请联系管理员 |
| 1000003 | 参数错误 | 结合参数错误的具体原因排查，比如页面大小不能大于100等 |
| 1000005 | 系统异常 | 尝试重试，如果还不行请联系管理员 |

#### 权限包
| 拥有此接口的权限包 | 可获得/可申请此权限包的应用类型 |
| --- | --- |
| 货品API组 | He uses type、Self use type |

### 2. bg.btg.goods.stock.warehouse.list.get - 根据站点查询可绑定的发货仓库信息接口

- 更新时间：2026-01-15 14:58:57
- 接口介绍：根据站点列表查询自发货模式品可绑定的发货仓信息

#### 请求地址
| 调用地址/地区 | 数据存储 |
| --- | --- |
| /openapi/router | CN |

#### 公共请求参数
| 参数接口 | 参数类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| type | STRING | 是 | API接口名, 形如:bg.* |
| app_key | STRING | 是 | 已创建成功的应用标志 |
| timestamp | STRING | 是 | 时间戳，格式为UNIX时间（秒） ，长度10位，当前时间-300秒<=入参时间<=当前时间+300秒 |
| sign | STRING | 是 | API入参参数签名，签名值根据如下算法给出计算过程 |
| data_type | STRING | 否 | 请求返回的数据格式，可选参数固定为JSON |
| access_token | STRING | 是 | 用户授权令牌access_token，卖家中心—授权管理，申请授权生成 |
| version | STRING | 是 | API版本，默认为V1，无要求不传此参数 |

#### 请求参数说明（树形层级）
  - `supplierId`: INTEGER，必填：是，说明：供应商id
- `siteIdList`: LIST，必填：是，说明：站点列表.
  - `$item`: INTEGER，必填：否，说明：-

#### 返回参数说明（树形层级）
  - `warehouseDTOList`: LIST，说明：站点可选发货仓列表
    - `$item`: OBJECT，说明：-
      - `validWarehouseList`: LIST，说明：可选发货仓列表
        - `$item`: OBJECT，说明：-
          - `warehouseDisable`: BOOLEAN，说明：仓库是否失效
          - `warehouseId`: STRING，说明：仓库id
          - `warehouseName`: STRING，说明：仓库名称
          - `managementType`: STRING，说明：仓库类型 0: 三方仓,1:自建仓,2:家庭仓,3:其他(仅适用于9个工作日发货时效的商品)
      - `siteId`: INTEGER，说明：站点id
      - `siteName`: STRING，说明：站点名称
- `success`: BOOLEAN，说明：status
- `errorCode`: INTEGER，说明：error code
- `errorMsg`: STRING，说明：error message

#### 返回错误码说明
| 错误码 | 错误描述 | 解决办法 |
| --- | --- | --- |
| 2000060 | 店铺类型不符合预期，不允许查询或变更库存操作 | 检查店铺ID |

#### 权限包
| 拥有此接口的权限包 | 可获得/可申请此权限包的应用类型 |
| --- | --- |
| 半托管库存API组 | He uses type、Self use type |
| 独立站高级接口 | He uses type、Self use type |

### 3. bg.glo.logistics.template.get - 查询运费模板列表

- 更新时间：2025-08-27 16:57:43
- 接口介绍：查询运费模版，用于半托管商品的API发布时关联运费模版ID

#### 请求地址
| 调用地址/地区 | 数据存储 |
| --- | --- |
| /openapi/router | CN |

#### 公共请求参数
| 参数接口 | 参数类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| type | STRING | 是 | API接口名, 形如:bg.* |
| app_key | STRING | 是 | 已创建成功的应用标志 |
| timestamp | STRING | 是 | 时间戳，格式为UNIX时间（秒） ，长度10位，当前时间-300秒<=入参时间<=当前时间+300秒 |
| sign | STRING | 是 | API入参参数签名，签名值根据如下算法给出计算过程 |
| data_type | STRING | 否 | 请求返回的数据格式，可选参数固定为JSON |
| access_token | STRING | 是 | 用户授权令牌access_token，卖家中心—授权管理，申请授权生成 |
| version | STRING | 是 | API版本，默认为V1，无要求不传此参数 |

#### 请求参数说明（树形层级）
  - `$item`: INTEGER，必填：否，说明：-

#### 返回参数说明（树形层级）
  - `freightTemplates`: LIST，说明：Freight Template Summary Information List
    - `$item`: OBJECT，说明：-
      - `freightTemplateId`: STRING，说明：Shipping Template ID
      - `templateName`: STRING，说明：Shipping Template Name
- `success`: BOOLEAN，说明：status
- `errorCode`: INTEGER，说明：error code
- `errorMsg`: STRING，说明：error message

#### 返回错误码说明
| 错误码 | 错误描述 | 解决办法 |
| --- | --- | --- |
| 2000124 | 查询运费模板信息失败 | 优先重试，如果不行请联系对接产品 |

#### 权限包
| 拥有此接口的权限包 | 可获得/可申请此权限包的应用类型 |
| --- | --- |
| 类目属性API组 | He uses type、Self use type |

### 4. bg.glo.product.search - 查询货品生命周期状态

- 更新时间：2025-12-23 16:02:46
- 接口介绍：外部erp系统，查询货品生命周期状态

#### 请求地址
| 调用地址/地区 | 数据存储 |
| --- | --- |
| /openapi/router | CN |

#### 公共请求参数
| 参数接口 | 参数类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| type | STRING | 是 | API接口名, 形如:bg.* |
| app_key | STRING | 是 | 已创建成功的应用标志 |
| timestamp | STRING | 是 | 时间戳，格式为UNIX时间（秒） ，长度10位，当前时间-300秒<=入参时间<=当前时间+300秒 |
| sign | STRING | 是 | API入参参数签名，签名值根据如下算法给出计算过程 |
| data_type | STRING | 否 | 请求返回的数据格式，可选参数固定为JSON |
| access_token | STRING | 是 | 用户授权令牌access_token，卖家中心—授权管理，申请授权生成 |
| version | STRING | 是 | API版本，默认为V1，无要求不传此参数 |

#### 请求参数说明（树形层级）
  - `$item`: INTEGER，必填：否，说明：-
- `pageSize`: INTEGER，必填：是，说明：页大小
- `pageNum`: INTEGER，必填：是，说明：页编号(从1开始)

#### 返回参数说明（树形层级）
  - `total`: INTEGER，说明：Total Count
  - `dataList`: LIST，说明：Data List
    - `$item`: OBJECT，说明：-
      - `skcList`: LIST，说明：SKC List
        - `$item`: OBJECT，说明：-
          - `skcId`: INTEGER，说明：Product SKC ID
          - `selectStatus`: INTEGER，说明：Selection Status
          - `skuList`: LIST，说明：SKU List
            - `$item`: OBJECT，说明：-
              - `skuId`: INTEGER，说明：Product SKU ID
          - `applyJitStatus`: INTEGER，说明：Appeal JIT Status (1-Application Allowed; 3-Application Not Allowed)
          - `suggestCloseJit`: BOOLEAN，说明：Whether to Suggest Closing JIT Button
      - `productId`: INTEGER，说明：Product ID
- `success`: BOOLEAN，说明：status
- `errorCode`: INTEGER，说明：error code
- `errorMsg`: STRING，说明：error message

#### 返回错误码说明
| 错误码 | 错误描述 | 解决办法 |
| --- | --- | --- |
| 2000000 | BUSINESS_EXCEPTION | 业务异常，可联系具体对接人员。 |
| 3000000 | pageSize is not null; pageSize max value 100; pageSize min value 1; 等等具体的参数异常信息。 | 参数异常，使用正确的参数。 |

#### 权限包
| 拥有此接口的权限包 | 可获得/可申请此权限包的应用类型 |
| --- | --- |
| 货品API组 | He uses type、Self use type |

### 5. bg.glo.goods.detail.get - 商品详情查询接口

- 更新时间：2025-08-27 16:57:39
- 接口介绍：查询商品详情信息

#### 请求地址
| 调用地址/地区 | 数据存储 |
| --- | --- |
| /openapi/router | CN |

#### 公共请求参数
| 参数接口 | 参数类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| type | STRING | 是 | API接口名, 形如:bg.* |
| app_key | STRING | 是 | 已创建成功的应用标志 |
| timestamp | STRING | 是 | 时间戳，格式为UNIX时间（秒） ，长度10位，当前时间-300秒<=入参时间<=当前时间+300秒 |
| sign | STRING | 是 | API入参参数签名，签名值根据如下算法给出计算过程 |
| data_type | STRING | 否 | 请求返回的数据格式，可选参数固定为JSON |
| access_token | STRING | 是 | 用户授权令牌access_token，卖家中心—授权管理，申请授权生成 |
| version | STRING | 是 | API版本，默认为V1，无要求不传此参数 |

#### 返回参数说明（树形层级）
  - `productId`: INTEGER，说明：Product ID
  - `categories`: OBJECT，说明：Category
    - `cat4`: OBJECT，说明：Fourth-Level Category
      - `catId`: INTEGER，说明：Category ID
      - `catName`: STRING，说明：Category Name
    - `cat5`: OBJECT，说明：Fifth-Level Category
      - `catId`: INTEGER，说明：Category ID
      - `catName`: STRING，说明：Category Name
    - `cat2`: OBJECT，说明：Secondary Category
      - `catId`: INTEGER，说明：Category ID
      - `catName`: STRING，说明：Category Name
    - `cat3`: OBJECT，说明：Third-Level Category
      - `catId`: INTEGER，说明：Category ID
      - `catName`: STRING，说明：Category Name
    - `cat1`: OBJECT，说明：First-Level Category
      - `catId`: INTEGER，说明：Category ID
      - `catName`: STRING，说明：Category Name
    - `cat10`: OBJECT，说明：Tenth-Level Category
      - `catId`: INTEGER，说明：Category ID
      - `catName`: STRING，说明：Category Name
    - `leafCat`: OBJECT，说明：Leaf Category
      - `catId`: INTEGER，说明：Category ID
      - `catName`: STRING，说明：Category Name
    - `catType`: INTEGER，说明：Category Type (0: Unclassified, 1: Apparel)
    - `cat8`: OBJECT，说明：8th Level Category
      - `catId`: INTEGER，说明：Category ID
      - `catName`: STRING，说明：Category Name
    - `cat9`: OBJECT，说明：Ninth-Level Category
      - `catId`: INTEGER，说明：Category ID
      - `catName`: STRING，说明：Category Name
    - `cat6`: OBJECT，说明：Sixth-Level Category
      - `catId`: INTEGER，说明：Category ID
      - `catName`: STRING，说明：Category Name
    - `cat7`: OBJECT，说明：Seventh-Level Category
      - `catId`: INTEGER，说明：Category ID
      - `catName`: STRING，说明：Category Name
  - `productWhExtAttr`: OBJECT，说明：Commodity Warehouse Logistics Supply Chain Extension Properties
    - `productOrigin`: OBJECT，说明：Product Origin Information
      - `region2Id`: INTEGER，说明：Secondary Region ID
      - `region1ShortName`: STRING，说明：First-Level Region Abbreviation (Two-Character Code)
  - `goodsLayerDecorationList`: LIST，说明：Product Detail Decoration Information
    - `$item`: OBJECT，说明：-
      - `floorId`: INTEGER，说明：Floor ID, null: Create new, otherwise Update
      - `lang`: STRING，说明：Language Type
      - `type`: STRING，说明：Component Type
      - `priority`: INTEGER，说明：Floor Sorting
      - `contentList`: LIST，说明：Floor Content
        - `$item`: OBJECT，说明：-
          - `imgUrl`: STRING，说明：Image Address--General
          - `textModuleDetails`: OBJECT，说明：Text Module Details
            - `backgroundColor`: STRING，说明：Background Color
            - `fontFamily`: INTEGER，说明：Font Type
            - `fontSize`: INTEGER，说明：Text Module Font Size
            - `align`: STRING，说明：Text Alignment, left--Left Aligned；right--Right Aligned；center--Centered；justify--Justified
            - `fontColor`: STRING，说明：Text Color
          - `width`: INTEGER，说明：Image Width--General
          - `text`: STRING，说明：Text Information--Text Module
          - `height`: INTEGER，说明：Image Height--General
      - `key`: STRING，说明：Floor Type Key
  - `productName`: STRING，说明：Product Name
- `success`: BOOLEAN，说明：status
- `errorCode`: INTEGER，说明：error code
- `errorMsg`: STRING，说明：error message

#### 返回错误码说明
| 错误码 | 错误描述 | 解决办法 |
| --- | --- | --- |
| 991000005 | 系统异常 | 系统异常，请联系管理员 |
| 991000011 | 无权访问 | 检查是否有该货品权限 |

#### 权限包
| 拥有此接口的权限包 | 可获得/可申请此权限包的应用类型 |
| --- | --- |
| 货品API组 | He uses type、Self use type |

### 6. bg.glo.goods.price.list.get - 货品供货价查询

- 更新时间：2025-08-27 16:57:39
- 接口介绍：供应商批量查询货品sku的供货价

#### 请求地址
| 调用地址/地区 | 数据存储 |
| --- | --- |
| /openapi/router | CN |

#### 公共请求参数
| 参数接口 | 参数类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| type | STRING | 是 | API接口名, 形如:bg.* |
| app_key | STRING | 是 | 已创建成功的应用标志 |
| timestamp | STRING | 是 | 时间戳，格式为UNIX时间（秒） ，长度10位，当前时间-300秒<=入参时间<=当前时间+300秒 |
| sign | STRING | 是 | API入参参数签名，签名值根据如下算法给出计算过程 |
| data_type | STRING | 否 | 请求返回的数据格式，可选参数固定为JSON |
| access_token | STRING | 是 | 用户授权令牌access_token，卖家中心—授权管理，申请授权生成 |
| version | STRING | 是 | API版本，默认为V1，无要求不传此参数 |

#### 请求参数说明（树形层级）
  - `$item`: INTEGER，必填：否，说明：-

#### 返回参数说明（树形层级）
  - `productSkuSupplierPriceList`: LIST，说明：Product SKU Supply Price List
    - `$item`: OBJECT，说明：-
      - `currencyType`: STRING，说明：Currency
      - `productSkuId`: INTEGER，说明：Product SKU ID
      - `productId`: INTEGER，说明：Product ID
      - `siteSupplierPrices`: LIST，说明：Site Supply Price List, only has value for semi_managed merchant
        - `$item`: OBJECT，说明：-
          - `priceReviewStatus`: INTEGER，说明：Pricing Status, may be empty for existing products or outside of grey area
          - `siteId`: INTEGER，说明：Site ID
          - `supplierPrice`: INTEGER，说明：Supply Price
      - `productSkcId`: INTEGER，说明：Product SKC ID
      - `supplierPrice`: INTEGER，说明：Supply Price, this value is deprecated in semi_managed merchant scenario
- `success`: BOOLEAN，说明：status
- `errorCode`: INTEGER，说明：error code
- `errorMsg`: STRING，说明：error message

#### 返回错误码说明
| 错误码 | 错误描述 | 解决办法 |
| --- | --- | --- |
| 1000011 | 无权访问 | 找对接产品申请加白 |

#### 权限包
| 拥有此接口的权限包 | 可获得/可申请此权限包的应用类型 |
| --- | --- |
| 价格API组 | He uses type、Self use type |
| 货品供货价API组 | He uses type、Self use type |

### 7. bg.semi.adjust.price.batch.review.order - 半托管批量确认/拒绝调价单

- 更新时间：2025-06-12 17:23:08
- 接口介绍：商家通过开平接口处理调价单

#### 请求地址
| 调用地址/地区 | 数据存储 |
| --- | --- |
| /openapi/router | CN |

#### 公共请求参数
| 参数接口 | 参数类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| type | STRING | 是 | API接口名, 形如:bg.* |
| app_key | STRING | 是 | 已创建成功的应用标志 |
| timestamp | STRING | 是 | 时间戳，格式为UNIX时间（秒） ，长度10位，当前时间-300秒<=入参时间<=当前时间+300秒 |
| sign | STRING | 是 | API入参参数签名，签名值根据如下算法给出计算过程 |
| data_type | STRING | 否 | 请求返回的数据格式，可选参数固定为JSON |
| access_token | STRING | 是 | 用户授权令牌access_token，卖家中心—授权管理，申请授权生成 |
| version | STRING | 是 | API版本，默认为V1，无要求不传此参数 |

#### 请求参数说明（树形层级）
- `submitOrders`: LIST，必填：是，说明：All Price Adjustment Orders for Batch Submission
  - `$item`: STRING，必填：否，说明：-
- `rejectReasons`: MAP，必填：否，说明：Rejection Reason, key=Price Adjustment Order Number, value=Reason
  - `$key`: STRING，必填：否，说明：-
  - `$value`: STRING，必填：否，说明：-

#### 返回参数说明（树形层级）
  - `successOrders`: LIST，说明：Submitted Price Adjustment Form Successfully
    - `$item`: STRING，说明：-
  - `failedOrders`: MAP，说明：Failed Price Adjustment Submission
    - `$key`: STRING，说明：-
    - `$value`: STRING，说明：-
- `success`: BOOLEAN，说明：status
- `errorCode`: INTEGER，说明：error code
- `errorMsg`: STRING，说明：error message

#### 返回错误码说明
| 错误码 | 错误描述 | 解决办法 |
| --- | --- | --- |
| 500000001 | 请求参数有误 | 核对接口入参是否正确 |
| 500000002 | 批量处理调价单失败 | 检查调价单状态 |

#### 权限包
| 拥有此接口的权限包 | 可获得/可申请此权限包的应用类型 |
| --- | --- |
| 调价API组 | He uses type、Self use type |

### 8. bg.semi.adjust.price.page.query.order - 分页查询半托管调价单

- 更新时间：2025-06-12 17:23:08
- 接口介绍：商家通过开平接口查询调价单

#### 请求地址
| 调用地址/地区 | 数据存储 |
| --- | --- |
| /openapi/router | CN |

#### 公共请求参数
| 参数接口 | 参数类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| type | STRING | 是 | API接口名, 形如:bg.* |
| app_key | STRING | 是 | 已创建成功的应用标志 |
| timestamp | STRING | 是 | 时间戳，格式为UNIX时间（秒） ，长度10位，当前时间-300秒<=入参时间<=当前时间+300秒 |
| sign | STRING | 是 | API入参参数签名，签名值根据如下算法给出计算过程 |
| data_type | STRING | 否 | 请求返回的数据格式，可选参数固定为JSON |
| access_token | STRING | 是 | 用户授权令牌access_token，卖家中心—授权管理，申请授权生成 |
| version | STRING | 是 | API版本，默认为V1，无要求不传此参数 |

#### 请求参数说明（树形层级）
  - `$item`: INTEGER，必填：否，说明：-
- `priceOrderSn`: LIST，必填：否，说明：Price Adjustment Order Number
  - `$item`: STRING，必填：否，说明：-
- `priceType`: INTEGER，必填：否，说明：Price Type. Optional value description:[0:Daily Price;1:Promotional Price;]
- `pageSize`: INTEGER，必填：是，说明：Page Size
- `source`: INTEGER，必填：否，说明：Application Source 1-Operation, 2-Supplier
- `trafficLowExpose`: BOOLEAN，必填：否，说明：Insufficient Traffic Exposure
- `pageNo`: INTEGER，必填：是，说明：Page Number
- `siteId`: INTEGER，必填：否，说明：Site ID
- `createdAtEnd`: INTEGER，必填：否，说明：Creation Date - End
- `createdAtBegin`: INTEGER，必填：否，说明：Creation Date - Start
- `status`: INTEGER，必填：否，说明：Status 0-Pending Pricing, 1-Pending Supplier Confirmation, 2-Pricing Successful, 3-Pricing Failed

#### 返回参数说明（树形层级）
  - `priceAdjustOrderList`: LIST，说明：-
    - `$item`: OBJECT，说明：-
      - `skcId`: INTEGER，说明：skc id
      - `priceOrderSn`: STRING，说明：Price Adjustment Order Number
      - `priceType`: INTEGER，说明：Price Type. Optional value description:[0:Daily Price;1:Promotional Price;]
      - `adjustReason`: STRING，说明：Price Adjustment Reason
      - `source`: STRING，说明：Source
      - `productName`: STRING，说明：Product Name
      - `skuInfoList`: LIST，说明：SKU Information
        - `$item`: OBJECT，说明：-
          - `productSkuId`: INTEGER，说明：sku id
          - `price`: INTEGER，说明：Original Currency Declaration Price
          - `spec`: STRING，说明：Specification Information
      - `newSupplyPrice`: STRING，说明：Reported Price After Price Adjustment
      - `priceCurrency`: STRING，说明：Currency for Supply
      - `rejectReason`: STRING，说明：Rejection Reason
      - `trafficLowExpose`: BOOLEAN，说明：Insufficient Traffic Exposure
      - `siteNameList`: LIST，说明：Semi-managed Merchant Bound Site Name List
        - `$item`: STRING，说明：-
      - `status`: INTEGER，说明：Price Adjustment Order Status, 0-Pending Pricing, 1-Pending Supplier Confirmation, 2-Price Adjustment Successful, 3-Price Adjustment Failed
  - `total`: INTEGER，说明：Total Count
- `success`: BOOLEAN，说明：status
- `errorCode`: INTEGER，说明：error code
- `errorMsg`: STRING，说明：error message

#### 返回错误码说明
| 错误码 | 错误描述 | 解决办法 |
| --- | --- | --- |
| 500000001 | 请求参数有误 | 核对接口入参是否正确 |

#### 权限包
| 拥有此接口的权限包 | 可获得/可申请此权限包的应用类型 |
| --- | --- |
| 调价API组 | He uses type、Self use type |

### 9. bg.semi.price.review.page.query.order - 分页查询半托管核价单

- 更新时间：2025-06-12 17:23:08
- 接口介绍：商家通过开平接口分页查询半托管核价单

#### 请求地址
| 调用地址/地区 | 数据存储 |
| --- | --- |
| /openapi/router | CN |

#### 公共请求参数
| 参数接口 | 参数类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| type | STRING | 是 | API接口名, 形如:bg.* |
| app_key | STRING | 是 | 已创建成功的应用标志 |
| timestamp | STRING | 是 | 时间戳，格式为UNIX时间（秒） ，长度10位，当前时间-300秒<=入参时间<=当前时间+300秒 |
| sign | STRING | 是 | API入参参数签名，签名值根据如下算法给出计算过程 |
| data_type | STRING | 否 | 请求返回的数据格式，可选参数固定为JSON |
| access_token | STRING | 是 | 用户授权令牌access_token，卖家中心—授权管理，申请授权生成 |
| version | STRING | 是 | API版本，默认为V1，无要求不传此参数 |

#### 请求参数说明（树形层级）
- `pageNo`: INTEGER，必填：是，说明：页码
- `orderStatusList`: LIST，必填：否，说明：核价单状态列表. 可选值含义说明:[0:待核价;1:待供应商确认;2:核价通过;3:核价驳回;4:废弃;5:价格同步中;]
  - `$item`: INTEGER，必填：否，说明：-
- `idGt`: INTEGER，必填：否，说明：id范围查询最小值
- `pageSize`: INTEGER，必填：是，说明：分页大小

#### 返回参数说明（树形层级）
  - `total`: INTEGER，说明：Total Count
  - `reviewSamplePriceList`: LIST，说明：Quote Sheet SKU and Reference Price
    - `$item`: OBJECT，说明：-
      - `productSkuIdList`: LIST，说明：Product SKU ID
        - `$item`: INTEGER，说明：-
      - `priceCurrency`: STRING，说明：Declared Price Currency. Only returns a value when the quotation status is 0, 2, or 3
      - `supplyPrice`: INTEGER，说明：Declared Price (cents). Only returns a value when the pricing status is 0, 2, or 3
      - `orderId`: INTEGER，说明：Quotation Sheet ID
      - `suggestPriceCurrency`: STRING，说明：Reference Price Currency
      - `suggestSupplyPrice`: INTEGER，说明：Reference Price (in cents)
      - `orderStatus`: INTEGER，说明：Quotation Status 0-Pending Quotation 1-Pending Supplier Confirmation 2-Quotation Approved 3-Quotation Rejected
      - `siteIds`: LIST，说明：Site ID
        - `$item`: INTEGER，说明：-
      - `canBargain`: BOOLEAN，说明：Whether Requotation is Allowed
      - `siteNameList`: LIST，说明：Site Name
        - `$item`: STRING，说明：-
- `success`: BOOLEAN，说明：status
- `errorCode`: INTEGER，说明：error code
- `errorMsg`: STRING，说明：error message

#### 返回错误码说明
| 错误码 | 错误描述 | 解决办法 |
| --- | --- | --- |
| 400000001 | 暂无权限查询 | 联系产品将店铺加灰度 |
| 400000002 | 仅支持半托管店铺查询 | 核验是否半托管店铺 |
| 400000003 | 页码不能超过X | 页码超过限制 |
| 400000010 | 查询待确认核价单失败 | 可以重试或联系产品排查原因 |

#### 权限包
| 拥有此接口的权限包 | 可获得/可申请此权限包的应用类型 |
| --- | --- |
| 核价API组 | He uses type、Self use type |

### 10. bg.semi.price.review.confirm.order - 半托管同意核价单建议价

- 更新时间：2025-06-12 17:23:08
- 接口介绍：商家同意半托管核价单建议价

#### 请求地址
| 调用地址/地区 | 数据存储 |
| --- | --- |
| /openapi/router | CN |

#### 公共请求参数
| 参数接口 | 参数类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| type | STRING | 是 | API接口名, 形如:bg.* |
| app_key | STRING | 是 | 已创建成功的应用标志 |
| timestamp | STRING | 是 | 时间戳，格式为UNIX时间（秒） ，长度10位，当前时间-300秒<=入参时间<=当前时间+300秒 |
| sign | STRING | 是 | API入参参数签名，签名值根据如下算法给出计算过程 |
| data_type | STRING | 否 | 请求返回的数据格式，可选参数固定为JSON |
| access_token | STRING | 是 | 用户授权令牌access_token，卖家中心—授权管理，申请授权生成 |
| version | STRING | 是 | API版本，默认为V1，无要求不传此参数 |

#### 返回错误码说明
| 错误码 | 错误描述 | 解决办法 |
| --- | --- | --- |
| 400000001 | 暂无权限查询 | 联系产品将店铺加灰度 |
| 400000004 | 缺少mallId | 核对入参店铺ID |
| 400000005 | 核价单不存在 | 核验入参核价单ID |
| 400000007 | 未查询到参考价格 | 核对核价单参考价格 |
| 400000010 | 查询待确认核价单失败 | 可以重试或联系产品排查原因 |

#### 权限包
| 拥有此接口的权限包 | 可获得/可申请此权限包的应用类型 |
| --- | --- |
| 核价API组 | He uses type、Self use type |

### 11. bg.semi.price.review.reject.order - 半托管不同意核价单建议价（并给出新的申报价）

- 更新时间：2025-06-12 17:23:08
- 接口介绍：商家不同意半托管核价单建议价（并给出新的申报价）

#### 请求地址
| 调用地址/地区 | 数据存储 |
| --- | --- |
| /openapi/router | CN |

#### 公共请求参数
| 参数接口 | 参数类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| type | STRING | 是 | API接口名, 形如:bg.* |
| app_key | STRING | 是 | 已创建成功的应用标志 |
| timestamp | STRING | 是 | 时间戳，格式为UNIX时间（秒） ，长度10位，当前时间-300秒<=入参时间<=当前时间+300秒 |
| sign | STRING | 是 | API入参参数签名，签名值根据如下算法给出计算过程 |
| data_type | STRING | 否 | 请求返回的数据格式，可选参数固定为JSON |
| access_token | STRING | 是 | 用户授权令牌access_token，卖家中心—授权管理，申请授权生成 |
| version | STRING | 是 | API版本，默认为V1，无要求不传此参数 |

#### 请求参数说明（树形层级）
  - `$item`: OBJECT，必填：否，说明：-
    - `componentList`: LIST，必填：是，说明：原因列表，允许录入1~20条
      - `$item`: OBJECT，必填：否，说明：-
        - `reason`: STRING，必填：是，说明：具体原因
        - `type`: INTEGER，必填：是，说明：重新报价原因类型. 可选值含义说明:[0:材质;1:功能;2:其他;3:品类;4:外观;5:版型;6:图案;7:规格尺寸;8:品牌;]
    - `externalLinkList`: LIST，必填：否，说明：外部链接，最多录入5个链接
      - `$item`: STRING，必填：否，说明：-
- `priceItemList`: LIST，必填：否，说明：-
  - `$item`: OBJECT，必填：否，说明：-
    - `productSkuId`: INTEGER，必填：是，说明：货品skuId
    - `price`: STRING，必填：是，说明：价格 分
- `orderId`: INTEGER，必填：是，说明：核价单id

#### 返回错误码说明
| 错误码 | 错误描述 | 解决办法 |
| --- | --- | --- |
| 400000001 | 暂无权限查询 | 联系产品将店铺加灰度 |
| 400000004 | 缺少mallId | 核对入参店铺ID |
| 400000005 | 核价单不存在 | 核验入参核价单ID |
| 400000006 | 无对应核价扩展单 | 核验入参核价单ID |
| 400000008 | 修改价格次数超过最大次数限制 | 不允许操作 |
| 400000010 | 查询待确认核价单失败 | 可以重试或联系产品排查原因 |

#### 权限包
| 拥有此接口的权限包 | 可获得/可申请此权限包的应用类型 |
| --- | --- |
| 核价API组 | He uses type、Self use type |

### 12. bg.marketing.activity.detail.get.global - 查询活动详情

- 更新时间：2025-07-30 17:56:41
- 接口介绍：营销活动商家报名

#### 请求地址
| 调用地址/地区 | 数据存储 |
| --- | --- |
| /openapi/router | CN |

#### 公共请求参数
| 参数接口 | 参数类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| type | STRING | 是 | API接口名, 形如:bg.* |
| app_key | STRING | 是 | 已创建成功的应用标志 |
| timestamp | STRING | 是 | 时间戳，格式为UNIX时间（秒） ，长度10位，当前时间-300秒<=入参时间<=当前时间+300秒 |
| sign | STRING | 是 | API入参参数签名，签名值根据如下算法给出计算过程 |
| data_type | STRING | 否 | 请求返回的数据格式，可选参数固定为JSON |
| access_token | STRING | 是 | 用户授权令牌access_token，卖家中心—授权管理，申请授权生成 |
| version | STRING | 是 | API版本，默认为V1，无要求不传此参数 |

#### 返回参数说明（树形层级）
  - `requirements`: LIST，说明：商品要求
    - `$item`: OBJECT，说明：-
      - `checkStatus`: INTEGER，说明：活动要求状态检查. 可选值含义说明:[1:符合;2:报名时检测;3:报名页面已自动过滤不满足的商品;0:不符合;]
      - `checkStatusDesc`: STRING，说明：活动要求状态检查
      - `requirementCode`: INTEGER，说明：活动要求枚举
      - `requirementType`: STRING，说明：活动要求类型
      - `requirementDesc`: STRING，说明：活动要求描述
  - `mallAptitude`: LIST，说明：店铺资质
    - `$item`: OBJECT，说明：-
      - `checkStatus`: INTEGER，说明：活动要求状态检查. 可选值含义说明:[1:符合;2:报名时检测;3:报名页面已自动过滤不满足的商品;0:不符合;]
      - `checkStatusDesc`: STRING，说明：活动要求状态检查
      - `requirementCode`: INTEGER，说明：活动要求枚举
      - `requirementType`: STRING，说明：活动要求类型
      - `requirementDesc`: STRING，说明：活动要求描述
  - `thematicInfo`: OBJECT，说明：主题信息
    - `benefitLabelName`: LIST，说明：权益标签
      - `$item`: STRING，说明：-
    - `durationDays`: INTEGER，说明：持续天数
    - `enrollSource`: INTEGER，说明：0-邀约报名 1-自主报名. 可选值含义说明:[0:邀约报名;1:自主报名;]
    - `salePromotionLabel`: STRING，说明：促销标签
    - `enrollDeadLine`: INTEGER，说明：报名截至时间
    - `activityLabelTag`: INTEGER，说明：活动标签tag. 可选值含义说明:[1:最新;2:爆款;]
    - `enrollStartAt`: INTEGER，说明：报名开始时间
    - `startTime`: INTEGER，说明：开始时间
    - `sites`: LIST，说明：Site, empty indicates no site restriction
      - `$item`: OBJECT，说明：-
        - `siteId`: INTEGER，说明：站点id
        - `siteName`: STRING，说明：站点名称
    - `endTime`: INTEGER，说明：结束时间
    - `activityThematicName`: STRING，说明：活动专题名称
    - `activityThematicId`: INTEGER，说明：活动专题id
  - `activityInfo`: OBJECT，说明：Activity Information
    - `benefitLabelName`: LIST，说明：权益标签
      - `$item`: STRING，说明：-
    - `activityContent`: STRING，说明：活动文案
    - `sessionAssignType`: INTEGER，说明：场次分配方式. 可选值含义说明:[1:时间周期内自动分配场次;2:用户选择场次;3:主题下自动分配场次;4:主题下报名全部场次;5:报名全部场次;]
    - `activityName`: STRING，说明：活动名称
    - `activityLabelTag`: INTEGER，说明：活动标签tag. 可选值含义说明:[1:最新;2:爆款;]
    - `activityType`: INTEGER，说明：活动类型 [13:大促进阶;1:限时秒杀;5:大促活动;27:清仓甩卖;101:秒杀进阶]
  - `canEnroll`: BOOLEAN，说明：是否可参加
- `success`: BOOLEAN，说明：status
- `errorCode`: INTEGER，说明：error code
- `errorMsg`: STRING，说明：error message

#### 返回错误码说明
| 错误码 | 错误描述 | 解决办法 |
| --- | --- | --- |
| 100000001 | 入参活动类型不能为空 | 检查入参 |

#### 权限包
| 拥有此接口的权限包 | 可获得/可申请此权限包的应用类型 |
| --- | --- |
| 活动API组 | He uses type、Self use type |

### 13. bg.marketing.activity.list.get.global - 查询活动列表

- 更新时间：2025-09-14 11:20:54
- 接口介绍：activityList

#### 请求地址
| 调用地址/地区 | 数据存储 |
| --- | --- |
| /openapi/router | CN |

#### 公共请求参数
| 参数接口 | 参数类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| type | STRING | 是 | API接口名, 形如:bg.* |
| app_key | STRING | 是 | 已创建成功的应用标志 |
| timestamp | STRING | 是 | 时间戳，格式为UNIX时间（秒） ，长度10位，当前时间-300秒<=入参时间<=当前时间+300秒 |
| sign | STRING | 是 | API入参参数签名，签名值根据如下算法给出计算过程 |
| data_type | STRING | 否 | 请求返回的数据格式，可选参数固定为JSON |
| access_token | STRING | 是 | 用户授权令牌access_token，卖家中心—授权管理，申请授权生成 |
| version | STRING | 是 | API版本，默认为V1，无要求不传此参数 |

#### 返回参数说明（树形层级）
  - `activityList`: LIST，说明：-
    - `$item`: OBJECT，说明：-
      - `benefitLabelName`: LIST，说明：权益标签
        - `$item`: STRING，说明：-
      - `activityContent`: STRING，说明：活动文案
      - `sessionAssignType`: INTEGER，说明：场次分配方式. [1:时间周期内自动分配场次;2:用户选择场次;3:主题下自动分配场次;4:主题下报名全部场次;5:报名全部场次;]
      - `activityName`: STRING，说明：活动名称
      - `activityLabelTag`: INTEGER，说明：活动标签tag
      - `thematicList`: LIST，说明：主题列表 [大促进阶和秒杀进阶使用]
        - `$item`: OBJECT，说明：-
          - `benefitLabelName`: LIST，说明：权益标签
            - `$item`: STRING，说明：-
          - `durationDays`: INTEGER，说明：持续天数
          - `enrollSource`: INTEGER，说明：0-邀约活动报名(已下线) 1-自主报名
          - `salePromotionLabel`: STRING，说明：促销标签
          - `enrollDeadLine`: INTEGER，说明：报名结束时间
          - `activityLabelTag`: INTEGER，说明：活动标签tag
          - `enrollStartAt`: INTEGER，说明：报名开始时间
          - `startTime`: INTEGER，说明：开始时间
          - `sites`: LIST，说明：站点列表
            - `$item`: OBJECT，说明：-
              - `siteId`: INTEGER，说明：站点id
              - `siteName`: STRING，说明：站点名称
          - `endTime`: INTEGER，说明：结束时间
          - `activityThematicName`: STRING，说明：活动主题名称
          - `activityThematicId`: INTEGER，说明：活动主题id
      - `activityType`: INTEGER，说明：活动类型. [13:大促进阶;1:限时秒杀;5:大促活动;27:清仓甩卖;101:秒杀进阶]
- `success`: BOOLEAN，说明：status
- `errorCode`: INTEGER，说明：error code
- `errorMsg`: STRING，说明：error message

#### 返回错误码说明
| 错误码 | 错误描述 | 解决办法 |
| --- | --- | --- |
| 100000001 | 入参活动类型不能为空 | 检查入参 |

#### 权限包
| 拥有此接口的权限包 | 可获得/可申请此权限包的应用类型 |
| --- | --- |
| 活动API组 | He uses type、Self use type |

### 14. bg.marketing.activity.product.get.global - 查询活动商品

- 更新时间：2025-07-30 17:56:41
- 接口介绍：营销活动查询报名商品列表

#### 请求地址
| 调用地址/地区 | 数据存储 |
| --- | --- |
| /openapi/router | CN |

#### 公共请求参数
| 参数接口 | 参数类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| type | STRING | 是 | API接口名, 形如:bg.* |
| app_key | STRING | 是 | 已创建成功的应用标志 |
| timestamp | STRING | 是 | 时间戳，格式为UNIX时间（秒） ，长度10位，当前时间-300秒<=入参时间<=当前时间+300秒 |
| sign | STRING | 是 | API入参参数签名，签名值根据如下算法给出计算过程 |
| data_type | STRING | 否 | 请求返回的数据格式，可选参数固定为JSON |
| access_token | STRING | 是 | 用户授权令牌access_token，卖家中心—授权管理，申请授权生成 |
| version | STRING | 是 | API版本，默认为V1，无要求不传此参数 |

#### 请求参数说明（树形层级）
- `productIds`: LIST，必填：否，说明：货品spuId
  - `$item`: INTEGER，必填：否，说明：-
- `productSkuExtCodes`: LIST，必填：否，说明：sku货号
  - `$item`: STRING，必填：否，说明：-
- `productSkcIds`: LIST，必填：否，说明：货品skcId
  - `$item`: INTEGER，必填：否，说明：-
- `productSkcExtCodes`: LIST，必填：否，说明：skc货号
  - `$item`: STRING，必填：否，说明：-
- `productSkuIds`: LIST，必填：否，说明：货品skuId
  - `$item`: INTEGER，必填：否，说明：-
- `siteId`: INTEGER，必填：否，说明：站点id
- `siteIds`: LIST，必填：否，说明：站点id列表
  - `$item`: INTEGER，必填：否，说明：-
- `rowCount`: INTEGER，必填：是，说明：行数
- `activityType`: INTEGER，必填：是，说明：活动类型. [13:大促进阶;1:限时秒杀;5:大促活动;27:清仓甩卖;101:秒杀进阶]
- `activityThematicId`: INTEGER，必填：否，说明：活动主题id（大促进阶、秒杀进阶使用）

#### 返回参数说明（树形层级）
  - `searchScrollContext`: STRING，说明：滚动查询上下文参数，用于下次查询（第一次查询或要重置查询时不需要传）
  - `matchList`: LIST，说明：符合报名条件的商品
    - `$item`: OBJECT，说明：-
      - `targetActivityStock`: INTEGER，说明：目标活动申报商品数（最低限制）
      - `skcList`: LIST，说明：skc
        - `$item`: OBJECT，说明：-
          - `skcId`: INTEGER，说明：skc id
          - `dailyPrice`: INTEGER，说明：（服饰类）日常申报价格， 全托管使用
          - `activityPrice`: INTEGER，说明：（服饰类）活动申报价格， 全托管使用
          - `skuList`: LIST，说明：sku
            - `$item`: OBJECT，说明：-
              - `dailyPrice`: INTEGER，说明：（非服饰类）日常申报价格， 全托管使用
              - `suggestActivityPrice`: INTEGER，说明：（非服饰类）建议申报价格， 全托管使用
              - `currency`: STRING，说明：币种
              - `sitePriceList`: LIST，说明：（非服饰类）各站点价格列表，半托管使用
                - `$item`: OBJECT，说明：-
                  - `dailyPrice`: INTEGER，说明：日常申报价格
                  - `suggestActivityPrice`: INTEGER，说明：建议申报价格
                  - `siteId`: INTEGER，说明：站点id
                  - `siteName`: STRING，说明：站点名称
              - `skuId`: INTEGER，说明：skuId
          - `suggestActivityPrice`: INTEGER，说明：（服饰类）建议申报价格， 全托管使用
          - `currency`: STRING，说明：币种
          - `sitePriceList`: LIST，说明：（服饰类）各站点价格列表，半托管使用
            - `$item`: OBJECT，说明：-
              - `dailyPrice`: INTEGER，说明：日常申报价格
              - `suggestActivityPrice`: INTEGER，说明：建议申报价格
              - `siteId`: INTEGER，说明：站点id
              - `siteName`: STRING，说明：站点名称
      - `productId`: INTEGER，说明：货品id
      - `enrollSessionIdList`: LIST，说明：已报名场次
        - `$item`: INTEGER，说明：-
      - `currency`: STRING，说明：币种:CNY-人民币元;USD-美元
      - `sites`: LIST，说明：商品经营站点
        - `$item`: OBJECT，说明：-
          - `siteId`: INTEGER，说明：站点id
          - `siteName`: STRING，说明：站点名称
      - `isApparel`: INTEGER，说明：是否服饰:1-是；0-否。服饰类使用skc，非服饰类使用sku
      - `productName`: STRING，说明：货品名称
      - `suggestActivityStock`: INTEGER，说明：建议活动申报商品数
  - `hasMore`: BOOLEAN，说明：是否存在更多数据
- `success`: BOOLEAN，说明：status
- `errorCode`: INTEGER，说明：error code
- `errorMsg`: STRING，说明：error message

#### 返回错误码说明
| 错误码 | 错误描述 | 解决办法 |
| --- | --- | --- |
| 100000001 | 入参活动类型不能为空 | 检查入参 |
| 100000003 | 入参行数不能为空 | 检查入参 |

#### 权限包
| 拥有此接口的权限包 | 可获得/可申请此权限包的应用类型 |
| --- | --- |
| 活动API组 | He uses type、Self use type |

### 15. bg.marketing.activity.session.list.get.global - 查询活动场次列表

- 更新时间：2025-07-30 17:56:41
- 接口介绍：营销活动查询活动场次列表接口

#### 请求地址
| 调用地址/地区 | 数据存储 |
| --- | --- |
| /openapi/router | CN |

#### 公共请求参数
| 参数接口 | 参数类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| type | STRING | 是 | API接口名, 形如:bg.* |
| app_key | STRING | 是 | 已创建成功的应用标志 |
| timestamp | STRING | 是 | 时间戳，格式为UNIX时间（秒） ，长度10位，当前时间-300秒<=入参时间<=当前时间+300秒 |
| sign | STRING | 是 | API入参参数签名，签名值根据如下算法给出计算过程 |
| data_type | STRING | 否 | 请求返回的数据格式，可选参数固定为JSON |
| access_token | STRING | 是 | 用户授权令牌access_token，卖家中心—授权管理，申请授权生成 |
| version | STRING | 是 | API版本，默认为V1，无要求不传此参数 |

#### 请求参数说明（树形层级）
  - `$item`: INTEGER，必填：否，说明：-
- `startTime`: INTEGER，必填：否，说明：开始时间
- `endTime`: INTEGER，必填：否，说明：结束时间
- `activityType`: INTEGER，必填：是，说明：活动类型 1-秒杀 5-官方大促 27-清仓 13-大促进阶（专题）
- `activityThematicId`: INTEGER，必填：否，说明：活动专题id，大促进阶使用

#### 返回参数说明（树形层级）
  - `siteIds`: LIST，说明：站点列表
    - `$item`: INTEGER，说明：-
  - `list`: LIST，说明：活动报名场次列表
    - `$item`: OBJECT，说明：-
      - `durationDays`: INTEGER，说明：场次持续天数
      - `startDateStr`: STRING，说明：场次开始日期字符串
      - `sessionName`: STRING，说明：场次名称
      - `sessionStatus`: INTEGER，说明：场次状态. 可选值含义说明:[1:未开始;2:进行中;3:活动已结束;4:报名失败;5:已售罄;6:已下线]
      - `siteId`: INTEGER，说明：站点id
      - `siteName`: STRING，说明：站点名称
      - `startTime`: INTEGER，说明：开始时间
      - `sessionId`: INTEGER，说明：场次id
      - `endTime`: INTEGER，说明：结束时间
      - `endDateStr`: STRING，说明：场次结束日期字符串
  - `productCanEnrollSessionMap`: MAP，说明：货品SPU可报名场次，Key为货品SPU ID
    - `$key`: STRING，说明：-
    - `$value`: LIST，说明：-
      - `$item`: OBJECT，说明：-
        - `durationDays`: INTEGER，说明：场次持续天数
        - `startDateStr`: STRING，说明：场次开始日期字符串
        - `sessionName`: STRING，说明：场次名称
        - `sessionStatus`: INTEGER，说明：场次状态. 可选值含义说明:[1:未开始;2:进行中;3:活动已结束;4:报名失败;5:已售罄;6:已下线]
        - `siteId`: INTEGER，说明：站点id
        - `siteName`: STRING，说明：站点名称
        - `startTime`: INTEGER，说明：开始时间
        - `sessionId`: INTEGER，说明：场次id
        - `endTime`: INTEGER，说明：结束时间
        - `endDateStr`: STRING，说明：场次结束日期字符串
- `success`: BOOLEAN，说明：status
- `errorCode`: INTEGER，说明：error code
- `errorMsg`: STRING，说明：error message

#### 返回错误码说明
| 错误码 | 错误描述 | 解决办法 |
| --- | --- | --- |
| 100000001 | 入参活动类型不能为空 | 检查入参 |
| 100000004 | 入参货品id不能为空 | 检查入参 |

#### 权限包
| 拥有此接口的权限包 | 可获得/可申请此权限包的应用类型 |
| --- | --- |
| 活动API组 | He uses type、Self use type |

### 16. bg.marketing.activity.enroll.list.get.global - 查询活动报名记录

- 更新时间：2025-07-30 17:56:41
- 接口介绍：活动报名记录查询接口

#### 请求地址
| 调用地址/地区 | 数据存储 |
| --- | --- |
| /openapi/router | CN |

#### 公共请求参数
| 参数接口 | 参数类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| type | STRING | 是 | API接口名, 形如:bg.* |
| app_key | STRING | 是 | 已创建成功的应用标志 |
| timestamp | STRING | 是 | 时间戳，格式为UNIX时间（秒） ，长度10位，当前时间-300秒<=入参时间<=当前时间+300秒 |
| sign | STRING | 是 | API入参参数签名，签名值根据如下算法给出计算过程 |
| data_type | STRING | 否 | 请求返回的数据格式，可选参数固定为JSON |
| access_token | STRING | 是 | 用户授权令牌access_token，卖家中心—授权管理，申请授权生成 |
| version | STRING | 是 | API版本，默认为V1，无要求不传此参数 |

#### 请求参数说明（树形层级）
- `sessionStatus`: INTEGER，必填：否，说明：1-未开始 2-进行中 3-已结束
- `pageSize`: INTEGER，必填：是，说明：page size
- `activityThematicId`: INTEGER，必填：否，说明：主题id [大促进阶和秒杀进阶使用]
- `enrollTimeEnd`: INTEGER，必填：否，说明：报名时间结束
- `productIds`: LIST，必填：否，说明：货品spuId
  - `$item`: INTEGER，必填：否，说明：-
- `productSkuExtCodes`: LIST，必填：否，说明：货品sku货号
  - `$item`: STRING，必填：否，说明：-
- `productSkcIds`: LIST，必填：否，说明：货品skcId
  - `$item`: INTEGER，必填：否，说明：-
- `pageNo`: INTEGER，必填：是，说明：页码
- `productSkcExtCodes`: LIST，必填：否，说明：货品skc货号
  - `$item`: STRING，必填：否，说明：-
- `productSkuIds`: LIST，必填：否，说明：货品skuId
  - `$item`: INTEGER，必填：否，说明：-
- `activityType`: INTEGER，必填：否，说明：活动类型. [13:大促进阶;1:限时秒杀;5:大促活动;27:清仓甩卖;101:秒杀进阶]

#### 返回参数说明（树形层级）
  - `total`: INTEGER，说明：total
  - `list`: LIST，说明：A list of event registration records
    - `$item`: OBJECT，说明：-
      - `assignSessionList`: LIST，说明：已分配活动场次
        - `$item`: OBJECT，说明：-
          - `durationDays`: INTEGER，说明：场次持续天数
          - `startDateStr`: STRING，说明：场次开始日期字符串，当地时区
          - `sessionName`: STRING，说明：场次名称
          - `sessionStatus`: INTEGER，说明：场次状态
          - `siteId`: INTEGER，说明：站点id
          - `siteName`: STRING，说明：站点名称
          - `startTime`: INTEGER，说明：开始时间
          - `sessionId`: INTEGER，说明：场次id
          - `endTime`: INTEGER，说明：结束时间
          - `endDateStr`: STRING，说明：场次结束日期字符串，当地时区
      - `productId`: INTEGER，说明：货品id
      - `goodsId`: INTEGER，说明：商品id
      - `activityTypeName`: STRING，说明：活动类型名称
      - `sessionEndTime`: INTEGER，说明：活动结束时间
      - `sessionStartTime`: INTEGER，说明：活动开始时间
      - `isApparel`: INTEGER，说明：是否服饰:1-是；0-否
      - `soldStatus`: INTEGER，说明：售罄状态 0-正常 1-即将售罄 2-已售罄
      - `remainingActivityStock`: INTEGER，说明：剩余活动库存
      - `activityThematicId`: INTEGER，说明：活动主题id
      - `enrollTime`: INTEGER，说明：报名时间
      - `activityStock`: INTEGER，说明：活动申报商品数
      - `skcList`: LIST，说明：skc列表
        - `$item`: OBJECT，说明：-
          - `skcId`: INTEGER，说明：skc id
          - `dailyPrice`: INTEGER，说明：（服饰类）日常申报价格，全托管
          - `activityPrice`: INTEGER，说明：（服饰类）活动申报价格，全托管
          - `skuList`: LIST，说明：sku
            - `$item`: OBJECT，说明：-
              - `dailyPrice`: INTEGER，说明：（非服饰类）日常申报价格，全托管
              - `activityPrice`: INTEGER，说明：（非服饰类）活动申报价格，全托管
              - `currency`: STRING，说明：币种:CNY-人民币元;USD-美元
              - `sitePriceList`: LIST，说明：（非服饰类）各站点申报价格，半托管
                - `$item`: OBJECT，说明：-
                  - `dailyPrice`: INTEGER，说明：日常申报价格
                  - `activityPrice`: INTEGER，说明：活动申报价格
                  - `siteId`: INTEGER，说明：站点id
                  - `siteName`: STRING，说明：站点名称
                  - `activityDiscount`: INTEGER，说明：活动申报折扣
              - `skuId`: INTEGER，说明：skuId
          - `currency`: STRING，说明：币种:CNY-人民币元;USD-美元
          - `sitePriceList`: LIST，说明：（服饰类）各站点申报价格，半托管
            - `$item`: OBJECT，说明：-
              - `dailyPrice`: INTEGER，说明：日常申报价格
              - `activityPrice`: INTEGER，说明：活动申报价格
              - `siteId`: INTEGER，说明：站点id
              - `siteName`: STRING，说明：站点名称
              - `activityDiscount`: INTEGER，说明：活动申报折扣
      - `enrollStatus`: INTEGER，说明：报名状态 1-报名中 2-报名失败 3-报名成功待分配场次 4-报名成功已分配场次 5-报名活动已结束 6-报名活动已下线
      - `enrollId`: INTEGER，说明：报名id
      - `currency`: STRING，说明：币种:CNY-人民币元;USD-美元
      - `activityType`: INTEGER，说明：活动类型
      - `activityThematicName`: STRING，说明：活动主题名称
- `success`: BOOLEAN，说明：status
- `errorCode`: INTEGER，说明：error code
- `errorMsg`: STRING，说明：error message

#### 返回错误码说明
| 错误码 | 错误描述 | 解决办法 |
| --- | --- | --- |
| 100000005 | 分页大小支持1-50 | 检查分页大小参数 |
| 100000006 | 页码参数非法 | 检查页码参数 |
| 100000007 | 活动类型参数非法 | 检查activityType参数 |
| 100000008 | 活动主题不存在 | 检查activityThematicId参数 |
| 100000009 | sessionStatus参数非法 | 检查sessionStatus参数 |

#### 权限包
| 拥有此接口的权限包 | 可获得/可申请此权限包的应用类型 |
| --- | --- |
| 活动API组 | He uses type、Self use type |

### 17. bg.marketing.activity.enroll.submit.global - 活动报名提交

- 更新时间：2025-07-30 17:56:41
- 接口介绍：营销活动报名提交

#### 请求地址
| 调用地址/地区 | 数据存储 |
| --- | --- |
| /openapi/router | CN |

#### 公共请求参数
| 参数接口 | 参数类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| type | STRING | 是 | API接口名, 形如:bg.* |
| app_key | STRING | 是 | 已创建成功的应用标志 |
| timestamp | STRING | 是 | 时间戳，格式为UNIX时间（秒） ，长度10位，当前时间-300秒<=入参时间<=当前时间+300秒 |
| sign | STRING | 是 | API入参参数签名，签名值根据如下算法给出计算过程 |
| data_type | STRING | 否 | 请求返回的数据格式，可选参数固定为JSON |
| access_token | STRING | 是 | 用户授权令牌access_token，卖家中心—授权管理，申请授权生成 |
| version | STRING | 是 | API版本，默认为V1，无要求不传此参数 |

#### 请求参数说明（树形层级）
- `productList`: LIST，必填：是，说明：报名货品列表
  - `$item`: OBJECT，必填：否，说明：-
    - `activityStock`: INTEGER，必填：是，说明：活动申报商品数
    - `skcList`: LIST，必填：是，说明：报名货品的skc列表
      - `$item`: OBJECT，必填：否，说明：-
        - `skcId`: INTEGER，必填：是，说明：skcId
        - `activityPrice`: INTEGER，必填：否，说明：skc活动价，当是服饰类
        - `skuList`: LIST，必填：是，说明：报名货品的sku列表
          - `$item`: OBJECT，必填：否，说明：-
            - `activityPrice`: INTEGER，必填：否，说明：sku活动价，当是非服饰类
            - `siteActivityPriceList`: LIST，必填：否，说明：sku各站点活动价，半托管传
              - `$item`: OBJECT，必填：否，说明：-
                - `activityPrice`: INTEGER，必填：否，说明：sku活动价
                - `siteId`: INTEGER，必填：是，说明：站点id
            - `skuId`: INTEGER，必填：是，说明：skuId
        - `siteActivityPriceList`: LIST，必填：否，说明：sku各站点活动价，半托管传
          - `$item`: OBJECT，必填：否，说明：-
            - `activityPrice`: INTEGER，必填：否，说明：skc活动价
            - `siteId`: INTEGER，必填：是，说明：站点id
    - `productId`: INTEGER，必填：是，说明：货品id
    - `sessionIds`: LIST，必填：否，说明：半托管需要传，全托管官方大促需要传
      - `$item`: INTEGER，必填：否，说明：-
- `activityThematicId`: INTEGER，必填：否，说明：主题列表 [大促进阶和秒杀进阶使用]

#### 返回参数说明（树形层级）
  - `failCount`: INTEGER，说明：失败数量
  - `successCount`: INTEGER，说明：成功数量
  - `failList`: LIST，说明：失败列表
    - `$item`: OBJECT，说明：-
      - `productId`: INTEGER，说明：货品id
      - `failMsg`: STRING，说明：失败描述
      - `failReason`: INTEGER，说明：失败原因
- `success`: BOOLEAN，说明：status
- `errorCode`: INTEGER，说明：error code
- `errorMsg`: STRING，说明：error message

#### 返回错误码说明
| 错误码 | 错误描述 | 解决办法 |
| --- | --- | --- |
| 100000001 | 入参活动类型不能为空 | 检查入参 |
| 100000002 | 入参货品列表不能为空 | 检查入参 |

#### 权限包
| 拥有此接口的权限包 | 可获得/可申请此权限包的应用类型 |
| --- | --- |
| 活动API组 | He uses type、Self use type |
