# TEMU 商品同步模块 — 实施方案

> 对应文档：新模块需求整理.md / 新模块数据库设计.md / 新模块同步方案.md

---

## 一、工程模块拆分

### 1.1 模块架构

```
temu-upin (parent)
├── temu-upin-sdk           # TEMU OpenAPI SDK（已有）
├── temu-upin-common        # 共享层：凭证、店铺、应用、平台配置
├── temu-upin-sync          # 新增：商品同步、核价、活动报名
└── temu-upin-backend       # 主启动模块（已有），依赖 common + sync
```

### 1.2 模块依赖关系

```
temu-upin-sdk        (无依赖)
temu-upin-common     → temu-upin-sdk, spring-boot-starter-web, spring-boot-starter-data-jpa
temu-upin-sync       → temu-upin-common, temu-upin-sdk, spring-boot-starter-data-redis
temu-upin-backend    → temu-upin-common, temu-upin-sync
```

### 1.3 包名规划

| 模块 | 基础包名 | 说明 |
|------|---------|------|
| temu-upin-common | `com.tminos.productscene` | 与原 backend 同包，无需改 import |
| temu-upin-sync | `com.tminos.productscene.sync` | 新包，自动被 Spring Boot 扫描 |
| temu-upin-backend | `com.tminos.productscene` | 保持不变 |

### 1.4 从 backend 移动到 common 的文件

| 类型 | 类名 |
|------|------|
| Entity | TemuShop, TemuSelfApp, PlatformConfigProfile, PlatformConfigItem |
| Repository | TemuShopRepository, TemuSelfAppRepository, PlatformConfigProfileRepository, PlatformConfigItemRepository |
| DTO | TemuShopDTO, TemuSelfAppDTO, PlatformConfigDTO |
| Service | TemuShopService, TemuSelfAppService, PlatformConfigService, TemuOpenApiCredentialService |
| Controller | TemuShopController, TemuSelfAppController, PlatformConfigController |

新增 `ApiResponse.java` 独立类（从 ProductDTO.ApiResponse 提取），供 common 和 sync 控制器使用。

---

## 二、新增功能实施

### 2.1 同步模块（temu-upin-sync）

#### 实体层
按 `新模块数据库设计.md` 中 23 张表创建 JPA 实体，JPA ddl-auto=update 自动建表。

#### 服务层
- **TemuSyncConfigService**：同步配置 CRUD，按店铺维度管理
- **TemuSyncLogService**：同步日志记录
- **TemuGoodsSyncService**：商品数据同步（按时间窗口分页下载→Redis暂存→批量入库）
- **TemuPriceReviewService**：核价单同步 + 自动审批 + 单个/批量审批
- **TemuPriceAdjustService**：调价单同步 + 批量审批
- **TemuActivityService**：活动同步 + 批量报名
- **WarehouseSyncExecutor**：发货仓同步，调用 `bg.btg.goods.stock.warehouse.list.get`，请求体固定使用店铺凭证中的店铺ID作为 `openApiUser.supplierId`，并传 `siteIdList=[100]`（美国站点），再将 `warehouseDTOList[].validWarehouseList` 展平入库

#### 商品同步策略
- 商品信息同步不再只按“已加站商品”拉取，改为基于 `bg.glo.goods.list.get` 的 `createdAtStart`、`createdAtEnd` 按时间范围同步。
- 手动同步支持两种方案：`最近一年` 用于初始化，`最近一周` 用于增量更新。
- `最近一年` 执行时按一周一周拆分时间窗口，每个时间窗口内部继续分页拉取，避免单周数据量过大导致遗漏。
- 自动同步和定时任务固定使用 `最近一周`，不允许自动触发 `最近一年` 初始化。
- 供货价同步改为从本地数据库中筛选“已加站商品”的 SKU 后，再批量调用 `bg.glo.goods.price.list.get`。

#### 控制器层
- `SyncConfigController`：同步配置管理 API
- `SyncTaskController`：同步任务触发 / 进度查询
- `TemuGoodsController`：商品列表 / 详情查询
- `PriceReviewController`：核价单列表 / 单个与批量审批
- `PriceAdjustController`：调价单列表 / 批量审批
- `ActivityController`：活动列表 / 批量报名

### 2.2 核价审核功能

- 页面展示核价单列表，每条核价单显示关联 SKU 详情（规格、价格、图片）
- 支持单个操作 + 批量选择 + 同意/驳回操作
- 同意调用 `bg.semi.price.review.confirm.order`
- 拒绝调用 `bg.semi.price.review.reject.order`
- 拒绝时支持填写 `bargainReasonList`、`externalLinkList` 与逐 SKU 改价 `priceItemList`
- 自动审批规则：低于阈值自动拒绝，高于阈值自动同意

### 2.3 批量报活动

- 页面展示可报名活动列表和场次信息
- 先选择活动 / 主题，再基于 `bg.marketing.activity.product.get.global` 过滤出真正可报名的商品
- 再基于 `bg.marketing.activity.session.list.get.global` 选择商品可用场次
- 按 `productList[].skcList[].skuList[]` 结构填写活动库存、活动价、站点价
- 调用 `bg.marketing.activity.enroll.submit.global` 批量提交报名
- 报名结果同步到本地 `temu_activity_enrollment` 与 `temu_activity_enroll_price` 表

#### 当前实现缺口

- 现有前端页面只支持查看活动和报名记录，还没有“商品选择 + 场次选择 + 批量提交”的报名工作台。
- 现有后端批量报名入参与正式接口不一致，不能直接用于生产报名。
- 现有同步闭环只覆盖了活动列表基础展示，未完成活动详情、可报名商品、场次、报名结果的完整串联。

---

## 三、Redis 配置

- `application-local.yml` 和 `application-dev.yml` 新增 Redis 连接配置
- 本地 Redis 无密码，默认端口 6379
- 用于同步任务暂存、分布式锁、进度追踪

---

## 四、前端新增页面

| 页面 | 路由 | 说明 |
|------|------|------|
| 同步配置 | /platform/sync-config | 按店铺配置同步参数 |
| 同步任务 | /platform/sync-tasks | 同步任务列表和进度 |
| 商品管理 | /platform/temu-goods | 商品列表和详情 |
| 核价审核 | /platform/price-review | 核价单列表 + 批量审批 |
| 调价审核 | /platform/price-adjust | 调价单列表 + 批量审批 |
| 活动报名 | /platform/sync-activity | 活动列表、报名记录、后续扩展批量报名工作台 |

---

## 五、实施步骤

1. 创建 temu-upin-common 模块结构和 POM
2. 创建 temu-upin-sync 模块结构和 POM
3. 更新父 POM 和 backend POM
4. 移动共享代码到 common、创建 ApiResponse
5. 添加 Redis 配置
6. 实现 sync 模块实体、仓库、服务、控制器
7. 实现商品同步两套方案与自动调度逻辑
8. 实现前端页面和路由
9. 构建验证
