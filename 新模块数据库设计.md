# TEMU 商品数据同步与管理模块 — 数据库设计

> 对应需求文档：`新模块需求整理.md`
> 数据库：PostgreSQL 14+
> 编码：UTF-8
> 所有金额字段单位为 **分（INTEGER）**，与 TEMU 接口保持一致

---

## 目录

1. [商品主表 temu_goods](#1-商品主表-temu_goods)
2. [商品属性表 temu_goods_property](#2-商品属性表-temu_goods_property)
3. [商品站点绑定表 temu_goods_site](#3-商品站点绑定表-temu_goods_site)
4. [商品 SKU 表 temu_goods_sku](#4-商品-sku-表-temu_goods_sku)
5. [SKU 规格表 temu_goods_sku_spec](#5-sku-规格表-temu_goods_sku_spec)
6. [SKU 条码表 temu_goods_sku_barcode](#6-sku-条码表-temu_goods_sku_barcode)
7. [SKU 供货价表 temu_goods_sku_price](#7-sku-供货价表-temu_goods_sku_price)
8. [SKU 站点价格表 temu_goods_sku_site_price](#8-sku-站点价格表-temu_goods_sku_site_price)
9. [商品生命周期状态表 temu_goods_lifecycle](#9-商品生命周期状态表-temu_goods_lifecycle)
10. [商品详情装修表 temu_goods_decoration](#10-商品详情装修表-temu_goods_decoration)
11. [运费模板表 temu_freight_template](#11-运费模板表-temu_freight_template)
12. [发货仓库表 temu_warehouse](#12-发货仓库表-temu_warehouse)
13. [调价单表 temu_price_adjust_order](#13-调价单表-temu_price_adjust_order)
14. [调价单 SKU 明细表 temu_price_adjust_sku](#14-调价单-sku-明细表-temu_price_adjust_sku)
15. [核价单表 temu_price_review_order](#15-核价单表-temu_price_review_order)
16. [核价单 SKU 明细表 temu_price_review_sku](#16-核价单-sku-明细表-temu_price_review_sku)
17. [营销活动表 temu_activity](#17-营销活动表-temu_activity)
18. [活动主题表 temu_activity_thematic](#18-活动主题表-temu_activity_thematic)
19. [活动场次表 temu_activity_session](#19-活动场次表-temu_activity_session)
20. [活动报名记录表 temu_activity_enrollment](#20-活动报名记录表-temu_activity_enrollment)
21. [活动报名 SKC/SKU 价格明细表 temu_activity_enroll_price](#21-活动报名-skcsku-价格明细表-temu_activity_enroll_price)
22. [同步配置表 temu_sync_config](#22-同步配置表-temu_sync_config)
23. [同步日志表 temu_sync_log](#23-同步日志表-temu_sync_log)

---

## ER 关系概览

```
temu_shops (已有)
  │
  ├── temu_goods ──┬── temu_goods_property
  │                ├── temu_goods_site
  │                ├── temu_goods_sku ──┬── temu_goods_sku_spec
  │                │                   ├── temu_goods_sku_barcode
  │                │                   ├── temu_goods_sku_price
  │                │                   └── temu_goods_sku_site_price
  │                ├── temu_goods_lifecycle
  │                └── temu_goods_decoration
  │
  ├── temu_freight_template
  ├── temu_warehouse
  │
  ├── temu_price_adjust_order ── temu_price_adjust_sku
  ├── temu_price_review_order ── temu_price_review_sku
  │
  ├── temu_activity ──┬── temu_activity_thematic
  │                   ├── temu_activity_session
  │                   └── temu_activity_enrollment ── temu_activity_enroll_price
  │
  ├── temu_sync_config
  └── temu_sync_log
```

---

## 1. 商品主表 temu_goods

> 数据来源：`bg.glo.goods.list.get`（API 1）、`bg.glo.goods.detail.get`（API 5）

| 字段名 | 类型 | 可空 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | BIGINT | 否 | GENERATED BY DEFAULT AS IDENTITY | 主键 |
| shop_id | VARCHAR(64) | 否 | | 店铺 ID（关联 temu_shops.shop_id） |
| product_id | BIGINT | 否 | | TEMU 商品 SPU ID（productId） |
| product_skc_id | BIGINT | 否 | | TEMU 商品 SKC ID（productSkcId） |
| product_name | VARCHAR(1000) | 否 | | 商品名称 |
| ext_code | VARCHAR(256) | 是 | NULL | SKC 外部编码 |
| main_image_url | VARCHAR(2000) | 是 | NULL | SKC 主图 URL |
| skc_site_status | INT | 是 | NULL | SKC 站点状态 |
| is_support_personalization | BOOLEAN | 是 | FALSE | 是否支持定制 |
| match_skc_jit_mode | BOOLEAN | 是 | FALSE | 是否命中 SKC 层 JIT 模式 |
| match_jit_mode | BOOLEAN | 是 | FALSE | 是否 JIT 模式 |
| sign_latest_jit_version | BOOLEAN | 是 | FALSE | 是否签署最新版 JIT 预售协议 |
| quick_sell_agt_sign_status | INT | 是 | NULL | 快销协议签署状态 0-未签 1-已签 |
| leaf_cat_id | INT | 是 | NULL | 叶子类目 ID |
| leaf_cat_name | VARCHAR(256) | 是 | NULL | 叶子类目名称 |
| cat_type | INT | 是 | NULL | 类目类型（0: 非服饰, 1: 服饰） |
| categories_json | JSONB | 是 | NULL | 完整类目层级 JSON（cat1~cat10 + leafCat） |
| freight_template_id | VARCHAR(128) | 是 | NULL | 运费模板 ID |
| shipment_limit_second | INT | 是 | NULL | 保证发货时间（秒） |
| long_transport | BOOLEAN | 是 | NULL | 长途运输模板 |
| warehouse_region_ids_json | JSONB | 是 | NULL | 发货仓一级区域 ID 列表 JSON |
| product_origin_json | JSONB | 是 | NULL | 商品产地信息 JSON |
| temu_created_at | BIGINT | 是 | NULL | TEMU 上架时间（UNIX 秒） |
| raw_json | JSONB | 是 | NULL | 接口原始返回 JSON（全量字段备份） |
| synced_at | TIMESTAMP | 是 | NULL | 最近一次同步时间 |
| created_at | TIMESTAMP | 否 | NOW() | 创建时间 |
| updated_at | TIMESTAMP | 否 | NOW() | 更新时间 |

**索引：**
- `UNIQUE INDEX uk_temu_goods_shop_skc (shop_id, product_skc_id)`
- `CREATE INDEX idx_temu_goods_shop (shop_id)`
- `CREATE INDEX idx_temu_goods_product (product_id)`
- `CREATE INDEX idx_temu_goods_name (product_name)`
- `CREATE INDEX idx_temu_goods_leaf_cat (leaf_cat_id)`
- `CREATE INDEX idx_temu_goods_synced (synced_at)`

---

## 2. 商品属性表 temu_goods_property

> 数据来源：`bg.glo.goods.list.get` 中 `productProperties` 字段

| 字段名 | 类型 | 可空 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | BIGINT | 否 | GENERATED BY DEFAULT AS IDENTITY | 主键 |
| goods_id | BIGINT | 否 | | 关联 temu_goods.id |
| pid | INT | 否 | | 属性 ID |
| template_pid | INT | 是 | NULL | 模板属性 ID |
| ref_pid | INT | 是 | NULL | 引用属性 ID |
| prop_name | VARCHAR(256) | 是 | NULL | 属性名称 |
| vid | INT | 是 | NULL | 属性值 ID |
| prop_value | VARCHAR(1000) | 是 | NULL | 属性值 |
| value_unit | VARCHAR(64) | 是 | NULL | 属性值单位 |
| value_extend_info | VARCHAR(1000) | 是 | NULL | 属性值扩展信息 |
| number_input_value | VARCHAR(256) | 是 | NULL | 数值输入 |
| language | VARCHAR(16) | 是 | NULL | 语言 |
| created_at | TIMESTAMP | 否 | NOW() | 创建时间 |
| updated_at | TIMESTAMP | 否 | NOW() | 更新时间 |

**索引：**
- `CREATE INDEX idx_temu_goods_property_goods (goods_id)`
- `CREATE INDEX idx_temu_goods_property_pid (pid)`

---

## 3. 商品站点绑定表 temu_goods_site

> 数据来源：`bg.glo.goods.list.get` 中 `productSemiManaged.bindSites` 字段

| 字段名 | 类型 | 可空 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | BIGINT | 否 | GENERATED BY DEFAULT AS IDENTITY | 主键 |
| goods_id | BIGINT | 否 | | 关联 temu_goods.id |
| site_id | INT | 否 | | 站点 ID |
| site_name | VARCHAR(128) | 是 | NULL | 站点名称 |
| created_at | TIMESTAMP | 否 | NOW() | 创建时间 |

**索引：**
- `UNIQUE INDEX uk_temu_goods_site (goods_id, site_id)`

---

## 4. 商品 SKU 表 temu_goods_sku

> 数据来源：`bg.glo.goods.list.get` 中 `productSkuSummaries` 字段

| 字段名 | 类型 | 可空 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | BIGINT | 否 | GENERATED BY DEFAULT AS IDENTITY | 主键 |
| goods_id | BIGINT | 否 | | 关联 temu_goods.id |
| shop_id | VARCHAR(64) | 否 | | 店铺 ID（冗余，便于查询） |
| product_sku_id | BIGINT | 否 | | TEMU SKU ID |
| ext_code | VARCHAR(256) | 是 | NULL | SKU 编码 |
| virtual_stock | INT | 是 | NULL | 虚拟库存 |
| weight_mg | INT | 是 | NULL | 重量（毫克） |
| wms_weight_mg | INT | 是 | NULL | WMS 重量（毫克） |
| length_mm | INT | 是 | NULL | 长度（mm） |
| width_mm | INT | 是 | NULL | 宽度（mm） |
| height_mm | INT | 是 | NULL | 高度（mm） |
| wms_length_mm | INT | 是 | NULL | WMS 长度（mm） |
| wms_width_mm | INT | 是 | NULL | WMS 宽度（mm） |
| wms_height_mm | INT | 是 | NULL | WMS 高度（mm） |
| wms_collection_source_type | INT | 是 | NULL | WMS 采集来源 |
| is_sensitive | INT | 是 | 0 | 是否敏感属性 0-非敏感 1-敏感 |
| sensitive_types_json | JSONB | 是 | NULL | 敏感类型列表 JSON |
| is_fragile | BOOLEAN | 是 | FALSE | 是否易碎品 |
| is_side_over_length | BOOLEAN | 是 | FALSE | 是否超长 |
| is_volume_over_size | BOOLEAN | 是 | FALSE | 是否超大体积 |
| is_force_to_normal | BOOLEAN | 是 | FALSE | 是否强制转普货 |
| shipping_mode | INT | 是 | NULL | 发货模式 |
| individually_packed | INT | 是 | NULL | 是否独立包装 |
| sub_sell_mode | INT | 是 | NULL | 子销售模式 |
| sensitive_limit_json | JSONB | 是 | NULL | 敏感属性限制 JSON（电池容量/液体容量/刀具长度等） |
| raw_json | JSONB | 是 | NULL | SKU 原始 JSON 备份 |
| created_at | TIMESTAMP | 否 | NOW() | 创建时间 |
| updated_at | TIMESTAMP | 否 | NOW() | 更新时间 |

**索引：**
- `UNIQUE INDEX uk_temu_goods_sku (shop_id, product_sku_id)`
- `CREATE INDEX idx_temu_goods_sku_goods (goods_id)`
- `CREATE INDEX idx_temu_goods_sku_ext_code (ext_code)`

---

## 5. SKU 规格表 temu_goods_sku_spec

> 数据来源：`productSkuSpecList` 字段

| 字段名 | 类型 | 可空 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | BIGINT | 否 | GENERATED BY DEFAULT AS IDENTITY | 主键 |
| sku_id | BIGINT | 否 | | 关联 temu_goods_sku.id |
| spec_id | INT | 否 | | 规格 ID |
| spec_name | VARCHAR(256) | 否 | | 规格名称 |
| parent_spec_id | INT | 否 | | 父规格 ID |
| parent_spec_name | VARCHAR(256) | 否 | | 父规格名称 |
| created_at | TIMESTAMP | 否 | NOW() | 创建时间 |

**索引：**
- `CREATE INDEX idx_temu_goods_sku_spec_sku (sku_id)`
- `CREATE INDEX idx_temu_goods_sku_spec_parent (parent_spec_id)`

---

## 6. SKU 条码表 temu_goods_sku_barcode

> 数据来源：`productSkuBarCodes` 字段

| 字段名 | 类型 | 可空 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | BIGINT | 否 | GENERATED BY DEFAULT AS IDENTITY | 主键 |
| sku_id | BIGINT | 否 | | 关联 temu_goods_sku.id |
| code | VARCHAR(128) | 否 | | 条码 |
| code_type | INT | 否 | | 条码类型（1: EAN, 2: UPC, 3: ISBN） |
| created_at | TIMESTAMP | 否 | NOW() | 创建时间 |

**索引：**
- `CREATE INDEX idx_temu_goods_sku_barcode_sku (sku_id)`
- `CREATE INDEX idx_temu_goods_sku_barcode_code (code)`

---

## 7. SKU 供货价表 temu_goods_sku_price

> 数据来源：`bg.glo.goods.price.list.get`（API 6）

| 字段名 | 类型 | 可空 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | BIGINT | 否 | GENERATED BY DEFAULT AS IDENTITY | 主键 |
| shop_id | VARCHAR(64) | 否 | | 店铺 ID |
| product_id | BIGINT | 否 | | TEMU 商品 SPU ID |
| product_skc_id | BIGINT | 否 | | TEMU 商品 SKC ID |
| product_sku_id | BIGINT | 否 | | TEMU SKU ID |
| supplier_price | INT | 是 | NULL | 供货价（分），半托管场景已废弃 |
| currency_type | VARCHAR(16) | 是 | NULL | 币种 |
| synced_at | TIMESTAMP | 是 | NULL | 最近同步时间 |
| created_at | TIMESTAMP | 否 | NOW() | 创建时间 |
| updated_at | TIMESTAMP | 否 | NOW() | 更新时间 |

**索引：**
- `UNIQUE INDEX uk_temu_sku_price (shop_id, product_sku_id)`
- `CREATE INDEX idx_temu_sku_price_product (product_id)`
- `CREATE INDEX idx_temu_sku_price_skc (product_skc_id)`

---

## 8. SKU 站点价格表 temu_goods_sku_site_price

> 数据来源：`bg.glo.goods.price.list.get` 中 `siteSupplierPrices`（半托管商家专用）

| 字段名 | 类型 | 可空 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | BIGINT | 否 | GENERATED BY DEFAULT AS IDENTITY | 主键 |
| sku_price_id | BIGINT | 否 | | 关联 temu_goods_sku_price.id |
| site_id | INT | 否 | | 站点 ID |
| supplier_price | INT | 是 | NULL | 该站点供货价（分） |
| price_review_status | INT | 是 | NULL | 核价状态 |
| created_at | TIMESTAMP | 否 | NOW() | 创建时间 |
| updated_at | TIMESTAMP | 否 | NOW() | 更新时间 |

**索引：**
- `UNIQUE INDEX uk_temu_sku_site_price (sku_price_id, site_id)`

---

## 9. 商品生命周期状态表 temu_goods_lifecycle

> 数据来源：`bg.glo.product.search`（API 4）

| 字段名 | 类型 | 可空 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | BIGINT | 否 | GENERATED BY DEFAULT AS IDENTITY | 主键 |
| shop_id | VARCHAR(64) | 否 | | 店铺 ID |
| product_id | BIGINT | 否 | | TEMU 商品 SPU ID |
| skc_id | BIGINT | 否 | | SKC ID |
| select_status | INT | 是 | NULL | 选品状态 |
| apply_jit_status | INT | 是 | NULL | JIT 申诉状态（1-允许申请; 3-不允许） |
| suggest_close_jit | BOOLEAN | 是 | FALSE | 是否建议关闭 JIT |
| sku_ids_json | JSONB | 是 | NULL | 该 SKC 下的 SKU ID 列表 JSON |
| synced_at | TIMESTAMP | 是 | NULL | 最近同步时间 |
| created_at | TIMESTAMP | 否 | NOW() | 创建时间 |
| updated_at | TIMESTAMP | 否 | NOW() | 更新时间 |

**索引：**
- `UNIQUE INDEX uk_temu_lifecycle_shop_skc (shop_id, skc_id)`
- `CREATE INDEX idx_temu_lifecycle_product (product_id)`
- `CREATE INDEX idx_temu_lifecycle_select (select_status)`

---

## 10. 商品详情装修表 temu_goods_decoration

> 数据来源：`bg.glo.goods.detail.get`（API 5）中 `goodsLayerDecorationList`

| 字段名 | 类型 | 可空 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | BIGINT | 否 | GENERATED BY DEFAULT AS IDENTITY | 主键 |
| goods_id | BIGINT | 否 | | 关联 temu_goods.id |
| floor_id | INT | 是 | NULL | 楼层 ID（null 表示新增） |
| floor_key | VARCHAR(128) | 是 | NULL | 楼层类型 Key |
| floor_type | VARCHAR(64) | 是 | NULL | 组件类型 |
| lang | VARCHAR(16) | 是 | NULL | 语言类型 |
| priority | INT | 是 | 0 | 楼层排序 |
| content_json | JSONB | 是 | NULL | 楼层内容 JSON（图文详情） |
| created_at | TIMESTAMP | 否 | NOW() | 创建时间 |
| updated_at | TIMESTAMP | 否 | NOW() | 更新时间 |

**索引：**
- `CREATE INDEX idx_temu_goods_decoration_goods (goods_id)`

---

## 11. 运费模板表 temu_freight_template

> 数据来源：`bg.glo.logistics.template.get`（API 3）

| 字段名 | 类型 | 可空 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | BIGINT | 否 | GENERATED BY DEFAULT AS IDENTITY | 主键 |
| shop_id | VARCHAR(64) | 否 | | 店铺 ID |
| freight_template_id | VARCHAR(128) | 否 | | TEMU 运费模板 ID |
| template_name | VARCHAR(256) | 是 | NULL | 运费模板名称 |
| synced_at | TIMESTAMP | 是 | NULL | 最近同步时间 |
| created_at | TIMESTAMP | 否 | NOW() | 创建时间 |
| updated_at | TIMESTAMP | 否 | NOW() | 更新时间 |

**索引：**
- `UNIQUE INDEX uk_temu_freight_tpl (shop_id, freight_template_id)`

---

## 12. 发货仓库表 temu_warehouse

> 数据来源：`bg.btg.goods.stock.warehouse.list.get`（API 2）

| 字段名 | 类型 | 可空 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | BIGINT | 否 | GENERATED BY DEFAULT AS IDENTITY | 主键 |
| shop_id | VARCHAR(64) | 否 | | 店铺 ID |
| site_id | INT | 否 | | 站点 ID |
| site_name | VARCHAR(128) | 是 | NULL | 站点名称 |
| warehouse_id | VARCHAR(128) | 否 | | 仓库 ID |
| warehouse_name | VARCHAR(256) | 是 | NULL | 仓库名称 |
| management_type | VARCHAR(16) | 是 | NULL | 仓库类型（0:三方仓, 1:自建仓, 2:家庭仓, 3:其他） |
| warehouse_disable | BOOLEAN | 是 | FALSE | 仓库是否失效 |
| synced_at | TIMESTAMP | 是 | NULL | 最近同步时间 |
| created_at | TIMESTAMP | 否 | NOW() | 创建时间 |
| updated_at | TIMESTAMP | 否 | NOW() | 更新时间 |

**索引：**
- `UNIQUE INDEX uk_temu_warehouse (shop_id, site_id, warehouse_id)`

---

## 13. 调价单表 temu_price_adjust_order

> 数据来源：`bg.semi.adjust.price.page.query.order`（API 8）

| 字段名 | 类型 | 可空 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | BIGINT | 否 | GENERATED BY DEFAULT AS IDENTITY | 主键 |
| shop_id | VARCHAR(64) | 否 | | 店铺 ID |
| price_order_sn | VARCHAR(128) | 否 | | 调价单号 |
| skc_id | BIGINT | 是 | NULL | SKC ID |
| product_name | VARCHAR(1000) | 是 | NULL | 商品名称 |
| price_type | INT | 是 | NULL | 价格类型（0:日常价, 1:活动价） |
| source | VARCHAR(32) | 是 | NULL | 来源（1-运营, 2-供应商） |
| adjust_reason | VARCHAR(2000) | 是 | NULL | 调价原因 |
| new_supply_price | VARCHAR(64) | 是 | NULL | 调价后申报价 |
| price_currency | VARCHAR(16) | 是 | NULL | 供货币种 |
| reject_reason | VARCHAR(2000) | 是 | NULL | 拒绝原因 |
| traffic_low_expose | BOOLEAN | 是 | FALSE | 流量曝光不足 |
| status | INT | 是 | NULL | 调价单状态（0-待核价, 1-待供应商确认, 2-调价成功, 3-调价失败） |
| site_names_json | JSONB | 是 | NULL | 绑定站点名称列表 JSON |
| review_action | VARCHAR(16) | 是 | NULL | 审批动作（APPROVE / REJECT / PENDING） |
| review_at | TIMESTAMP | 是 | NULL | 审批时间 |
| synced_at | TIMESTAMP | 是 | NULL | 最近同步时间 |
| created_at | TIMESTAMP | 否 | NOW() | 创建时间 |
| updated_at | TIMESTAMP | 否 | NOW() | 更新时间 |

**索引：**
- `UNIQUE INDEX uk_temu_price_adjust (shop_id, price_order_sn)`
- `CREATE INDEX idx_temu_price_adjust_status (status)`
- `CREATE INDEX idx_temu_price_adjust_skc (skc_id)`
- `CREATE INDEX idx_temu_price_adjust_review (review_action)`

---

## 14. 调价单 SKU 明细表 temu_price_adjust_sku

> 数据来源：`bg.semi.adjust.price.page.query.order` 中 `skuInfoList`

| 字段名 | 类型 | 可空 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | BIGINT | 否 | GENERATED BY DEFAULT AS IDENTITY | 主键 |
| adjust_order_id | BIGINT | 否 | | 关联 temu_price_adjust_order.id |
| product_sku_id | BIGINT | 否 | | TEMU SKU ID |
| price | INT | 是 | NULL | 原币申报价（分） |
| spec | VARCHAR(2000) | 是 | NULL | 规格信息 |
| created_at | TIMESTAMP | 否 | NOW() | 创建时间 |

**索引：**
- `CREATE INDEX idx_temu_price_adjust_sku_order (adjust_order_id)`
- `CREATE INDEX idx_temu_price_adjust_sku_sku (product_sku_id)`

---

## 15. 核价单表 temu_price_review_order

> 数据来源：`bg.semi.price.review.page.query.order`（API 9）

| 字段名 | 类型 | 可空 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | BIGINT | 否 | GENERATED BY DEFAULT AS IDENTITY | 主键 |
| shop_id | VARCHAR(64) | 否 | | 店铺 ID |
| order_id | BIGINT | 否 | | TEMU 核价单 ID |
| order_status | INT | 是 | NULL | 核价状态（0-待核价, 1-待供应商确认, 2-核价通过, 3-核价驳回, 4-废弃, 5-价格同步中） |
| supply_price | INT | 是 | NULL | 申报价（分） |
| price_currency | VARCHAR(16) | 是 | NULL | 申报价币种 |
| suggest_supply_price | INT | 是 | NULL | 建议供货价（分） |
| suggest_price_currency | VARCHAR(16) | 是 | NULL | 建议价币种 |
| can_bargain | BOOLEAN | 是 | FALSE | 是否允许重新报价 |
| site_ids_json | JSONB | 是 | NULL | 站点 ID 列表 JSON |
| site_names_json | JSONB | 是 | NULL | 站点名称列表 JSON |
| review_action | VARCHAR(16) | 是 | NULL | 审批动作（APPROVE / REJECT / PENDING） |
| review_at | TIMESTAMP | 是 | NULL | 审批时间 |
| reject_reasons_json | JSONB | 是 | NULL | 拒绝原因 JSON（完整 `bargainReasonList`，含 componentList / externalLinkList） |
| synced_at | TIMESTAMP | 是 | NULL | 最近同步时间 |
| created_at | TIMESTAMP | 否 | NOW() | 创建时间 |
| updated_at | TIMESTAMP | 否 | NOW() | 更新时间 |

**索引：**
- `UNIQUE INDEX uk_temu_price_review (shop_id, order_id)`
- `CREATE INDEX idx_temu_price_review_status (order_status)`
- `CREATE INDEX idx_temu_price_review_action (review_action)`

---

## 16. 核价单 SKU 明细表 temu_price_review_sku

> 数据来源：`bg.semi.price.review.page.query.order` 中 `productSkuIdList` + `bg.semi.price.review.reject.order` 中 `priceItemList`

| 字段名 | 类型 | 可空 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | BIGINT | 否 | GENERATED BY DEFAULT AS IDENTITY | 主键 |
| review_order_id | BIGINT | 否 | | 关联 temu_price_review_order.id |
| product_sku_id | BIGINT | 否 | | TEMU SKU ID |
| new_price | INT | 是 | NULL | 拒绝时给出的新申报价（分） |
| created_at | TIMESTAMP | 否 | NOW() | 创建时间 |

**索引：**
- `CREATE INDEX idx_temu_price_review_sku_order (review_order_id)`

---

## 17. 营销活动表 temu_activity

> 数据来源：`bg.marketing.activity.list.get.global`（API 13）

| 字段名 | 类型 | 可空 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | BIGINT | 否 | GENERATED BY DEFAULT AS IDENTITY | 主键 |
| shop_id | VARCHAR(64) | 否 | | 店铺 ID |
| activity_type | INT | 否 | | 活动类型（1:限时秒杀, 5:大促活动, 13:大促进阶, 27:清仓甩卖, 101:秒杀进阶） |
| activity_name | VARCHAR(512) | 是 | NULL | 活动名称 |
| activity_content | TEXT | 是 | NULL | 活动文案 |
| activity_label_tag | INT | 是 | NULL | 活动标签 tag（1:最新, 2:爆款） |
| session_assign_type | INT | 是 | NULL | 场次分配方式（1~5） |
| benefit_labels_json | JSONB | 是 | NULL | 权益标签列表 JSON |
| synced_at | TIMESTAMP | 是 | NULL | 最近同步时间 |
| created_at | TIMESTAMP | 否 | NOW() | 创建时间 |
| updated_at | TIMESTAMP | 否 | NOW() | 更新时间 |

**索引：**
- `CREATE INDEX idx_temu_activity_shop (shop_id)`
- `CREATE INDEX idx_temu_activity_type (activity_type)`

---

## 18. 活动主题表 temu_activity_thematic

> 数据来源：`bg.marketing.activity.list.get.global` 中 `thematicList` + `bg.marketing.activity.detail.get.global` 中 `thematicInfo`

| 字段名 | 类型 | 可空 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | BIGINT | 否 | GENERATED BY DEFAULT AS IDENTITY | 主键 |
| activity_id | BIGINT | 否 | | 关联 temu_activity.id |
| activity_thematic_id | BIGINT | 否 | | TEMU 活动主题 ID |
| activity_thematic_name | VARCHAR(512) | 是 | NULL | 活动主题名称 |
| enroll_source | INT | 是 | NULL | 报名来源（0-邀约, 1-自主） |
| enroll_start_at | BIGINT | 是 | NULL | 报名开始时间（UNIX 秒） |
| enroll_dead_line | BIGINT | 是 | NULL | 报名截止时间（UNIX 秒） |
| start_time | BIGINT | 是 | NULL | 活动开始时间（UNIX 秒） |
| end_time | BIGINT | 是 | NULL | 活动结束时间（UNIX 秒） |
| duration_days | INT | 是 | NULL | 持续天数 |
| sale_promotion_label | VARCHAR(256) | 是 | NULL | 促销标签 |
| activity_label_tag | INT | 是 | NULL | 活动标签 tag |
| benefit_labels_json | JSONB | 是 | NULL | 权益标签 JSON |
| sites_json | JSONB | 是 | NULL | 站点限制列表 JSON |
| created_at | TIMESTAMP | 否 | NOW() | 创建时间 |
| updated_at | TIMESTAMP | 否 | NOW() | 更新时间 |

**索引：**
- `CREATE INDEX idx_temu_activity_thematic_act (activity_id)`
- `UNIQUE INDEX uk_temu_activity_thematic (activity_id, activity_thematic_id)`

---

## 19. 活动场次表 temu_activity_session

> 数据来源：`bg.marketing.activity.session.list.get.global`（API 15）

| 字段名 | 类型 | 可空 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | BIGINT | 否 | GENERATED BY DEFAULT AS IDENTITY | 主键 |
| shop_id | VARCHAR(64) | 否 | | 店铺 ID |
| activity_type | INT | 否 | | 活动类型 |
| activity_thematic_id | BIGINT | 是 | NULL | 活动主题 ID（大促进阶等使用） |
| session_id | BIGINT | 否 | | 场次 ID |
| session_name | VARCHAR(256) | 是 | NULL | 场次名称 |
| session_status | INT | 是 | NULL | 场次状态（1:未开始, 2:进行中, 3:已结束, 4:报名失败, 5:已售罄, 6:已下线） |
| site_id | INT | 是 | NULL | 站点 ID |
| site_name | VARCHAR(128) | 是 | NULL | 站点名称 |
| start_time | BIGINT | 是 | NULL | 开始时间（UNIX 秒） |
| end_time | BIGINT | 是 | NULL | 结束时间（UNIX 秒） |
| start_date_str | VARCHAR(32) | 是 | NULL | 开始日期字符串 |
| end_date_str | VARCHAR(32) | 是 | NULL | 结束日期字符串 |
| duration_days | INT | 是 | NULL | 持续天数 |
| synced_at | TIMESTAMP | 是 | NULL | 最近同步时间 |
| created_at | TIMESTAMP | 否 | NOW() | 创建时间 |
| updated_at | TIMESTAMP | 否 | NOW() | 更新时间 |

**索引：**
- `UNIQUE INDEX uk_temu_activity_session (shop_id, session_id)`
- `CREATE INDEX idx_temu_activity_session_type (activity_type)`
- `CREATE INDEX idx_temu_activity_session_status (session_status)`

---

## 20. 活动报名记录表 temu_activity_enrollment

> 数据来源：`bg.marketing.activity.enroll.list.get.global`（API 16）

| 字段名 | 类型 | 可空 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | BIGINT | 否 | GENERATED BY DEFAULT AS IDENTITY | 主键 |
| shop_id | VARCHAR(64) | 否 | | 店铺 ID |
| enroll_id | BIGINT | 否 | | TEMU 报名 ID |
| product_id | BIGINT | 否 | | 商品 SPU ID |
| goods_id | BIGINT | 是 | NULL | 商品 ID |
| activity_type | INT | 是 | NULL | 活动类型 |
| activity_type_name | VARCHAR(128) | 是 | NULL | 活动类型名称 |
| activity_thematic_id | BIGINT | 是 | NULL | 活动主题 ID |
| activity_thematic_name | VARCHAR(512) | 是 | NULL | 活动主题名称 |
| enroll_status | INT | 是 | NULL | 报名状态（1-报名中, 2-报名失败, 3-报名成功待分配场次, 4-报名成功已分配场次, 5-活动已结束, 6-活动已下线） |
| enroll_time | BIGINT | 是 | NULL | 报名时间（UNIX 秒） |
| activity_stock | INT | 是 | NULL | 活动申报商品数 |
| remaining_activity_stock | INT | 是 | NULL | 剩余活动库存 |
| is_apparel | INT | 是 | NULL | 是否服饰（1-是, 0-否） |
| currency | VARCHAR(16) | 是 | NULL | 币种 |
| sold_status | INT | 是 | NULL | 售罄状态（0-正常, 1-即将售罄, 2-已售罄） |
| session_start_time | BIGINT | 是 | NULL | 活动场次开始时间 |
| session_end_time | BIGINT | 是 | NULL | 活动场次结束时间 |
| assign_sessions_json | JSONB | 是 | NULL | 已分配场次列表 JSON |
| synced_at | TIMESTAMP | 是 | NULL | 最近同步时间 |
| created_at | TIMESTAMP | 否 | NOW() | 创建时间 |
| updated_at | TIMESTAMP | 否 | NOW() | 更新时间 |

**索引：**
- `UNIQUE INDEX uk_temu_enrollment (shop_id, enroll_id)`
- `CREATE INDEX idx_temu_enrollment_product (product_id)`
- `CREATE INDEX idx_temu_enrollment_status (enroll_status)`
- `CREATE INDEX idx_temu_enrollment_type (activity_type)`

---

## 21. 活动报名 SKC/SKU 价格明细表 temu_activity_enroll_price

> 数据来源：`bg.marketing.activity.enroll.list.get.global` 中 `skcList` / `skuList` / `sitePriceList`

| 字段名 | 类型 | 可空 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | BIGINT | 否 | GENERATED BY DEFAULT AS IDENTITY | 主键 |
| enrollment_id | BIGINT | 否 | | 关联 temu_activity_enrollment.id |
| level | VARCHAR(8) | 否 | | 层级（SKC / SKU） |
| skc_id | BIGINT | 是 | NULL | SKC ID（level=SKC 或作为 SKU 的父级） |
| sku_id | BIGINT | 是 | NULL | SKU ID（level=SKU 时填写） |
| site_id | INT | 是 | NULL | 站点 ID（半托管站点价格时填写） |
| site_name | VARCHAR(128) | 是 | NULL | 站点名称 |
| daily_price | INT | 是 | NULL | 日常申报价（分） |
| activity_price | INT | 是 | NULL | 活动申报价（分） |
| activity_discount | INT | 是 | NULL | 活动申报折扣 |
| currency | VARCHAR(16) | 是 | NULL | 币种 |
| created_at | TIMESTAMP | 否 | NOW() | 创建时间 |

**索引：**
- `CREATE INDEX idx_temu_enroll_price_enrollment (enrollment_id)`
- `CREATE INDEX idx_temu_enroll_price_skc (skc_id)`
- `CREATE INDEX idx_temu_enroll_price_sku (sku_id)`

---

## 22. 同步配置表 temu_sync_config

> 数据来源：业务需求 1.3 同步配置模块

| 字段名 | 类型 | 可空 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | BIGINT | 否 | GENERATED BY DEFAULT AS IDENTITY | 主键 |
| shop_id | VARCHAR(64) | 否 | | 店铺 ID（每个店铺独立配置） |
| config_key | VARCHAR(128) | 否 | | 配置项 Key |
| config_value | VARCHAR(2000) | 是 | NULL | 配置项值 |
| config_desc | VARCHAR(512) | 是 | NULL | 配置项说明 |
| created_at | TIMESTAMP | 否 | NOW() | 创建时间 |
| updated_at | TIMESTAMP | 否 | NOW() | 更新时间 |

**预置配置项（config_key）：**

| config_key | 默认值 | 说明 |
|-----------|--------|------|
| `goods_sync_thread_count` | 5 | 商品信息同步线程数 |
| `lifecycle_sync_thread_count` | 5 | 商品状态同步线程数 |
| `price_sync_thread_count` | 3 | 价格同步线程数 |
| `sync_cron` | `0 2 * * *` | 每日同步 cron 表达式 |
| `price_review_auto_reject_threshold` | 2800 | 核价自动拒绝阈值（分） |
| `price_review_auto_approve_threshold` | | 核价自动同意阈值（分） |

**索引：**
- `UNIQUE INDEX uk_temu_sync_config (shop_id, config_key)`

---

## 23. 同步日志表 temu_sync_log

> 业务需要：记录每次同步任务的执行情况

| 字段名 | 类型 | 可空 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | BIGINT | 否 | GENERATED BY DEFAULT AS IDENTITY | 主键 |
| shop_id | VARCHAR(64) | 否 | | 店铺 ID |
| sync_type | VARCHAR(64) | 否 | | 同步类型（GOODS / LIFECYCLE / PRICE / FREIGHT / WAREHOUSE / ACTIVITY / PRICE_ADJUST / PRICE_REVIEW） |
| status | VARCHAR(16) | 否 | STARTED | 状态（STARTED / SUCCEEDED / FAILED） |
| total_count | INT | 是 | 0 | 同步总数 |
| success_count | INT | 是 | 0 | 成功数 |
| fail_count | INT | 是 | 0 | 失败数 |
| error_msg | TEXT | 是 | NULL | 错误信息 |
| started_at | TIMESTAMP | 否 | | 开始时间 |
| finished_at | TIMESTAMP | 是 | NULL | 结束时间 |
| created_at | TIMESTAMP | 否 | NOW() | 创建时间 |

**索引：**
- `CREATE INDEX idx_temu_sync_log_shop (shop_id)`
- `CREATE INDEX idx_temu_sync_log_type (sync_type)`
- `CREATE INDEX idx_temu_sync_log_status (status)`
- `CREATE INDEX idx_temu_sync_log_started (started_at)`

---

## 设计原则说明

### 1. 店铺隔离
所有表均包含 `shop_id` 字段，关联已有 `temu_shops.shop_id`。每次 SDK 调用凭证（access_token）均对应具体店铺，数据天然按店铺隔离。

### 2. 全量字段存储
商品主表 `temu_goods` 和 SKU 表 `temu_goods_sku` 均含 `raw_json` 字段，用于存储接口原始返回 JSON。即使某些字段未拆分为独立列，也可通过 `raw_json` 追溯完整数据，满足"拉取到的每一个字段都必须完整落库"的需求。

### 3. 核价自动审批
- `temu_price_adjust_order` 和 `temu_price_review_order` 均包含 `review_action` 字段，用于记录系统审批动作。
- 同步拉取到新的调价单/核价单后，系统根据 `temu_sync_config` 中的阈值配置自动填充 `review_action`，超出阈值范围的进入人工审核队列（`review_action = 'PENDING'`）。

### 4. 同步幂等
所有与 TEMU 接口数据对应的表均设有唯一约束（如 `shop_id + product_skc_id`、`shop_id + price_order_sn` 等），同步时使用 INSERT ... ON CONFLICT ... DO UPDATE (UPSERT) 语义，保证重复同步的幂等性。

### 5. 活动数据三层结构
- **活动 → 主题 → 场次**：`temu_activity` → `temu_activity_thematic` → `temu_activity_session`
- **报名 → 价格明细**：`temu_activity_enrollment` → `temu_activity_enroll_price`
- 价格明细表通过 `level` 字段区分 SKC 维度和 SKU 维度的价格，支持服饰类（SKC 级别定价）和非服饰类（SKU 级别定价）两种模式。

### 6. 类目存储策略
TEMU 类目最多 10 级，独立拆字段意义不大。设计中叶子类目单独拆列（`leaf_cat_id` / `leaf_cat_name`）用于查询和索引，完整类目层级存入 `categories_json` JSON 字段。
